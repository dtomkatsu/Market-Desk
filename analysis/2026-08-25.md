# 2026-08-25

HE posted the board's first `washout` divergence verdict in this note series — a fourth straight session of losses on above-average volume with almost nothing left to sell — while GLD held its `confirmed` read for a second session and every other name on the 42-symbol board settled into a noticeably quieter turnover pattern than the last few sessions.

## Volume

**HE** flipped to `washout`: `rvol` 0.935 (below its own recent average, not elevated), -0.35% on the day, -1.64% for the week, but -13.99% for the month, `up_down_ratio` 0.566 (down-volume days dominating), RSI14 26.63, still below both its 50- and 200-day averages. A washout reads as selling pressure exhausting itself on a stock already down heavily — not a bottom call, just the heuristic's label for this shape of move.

**GLD** stayed `confirmed` for a second straight session, though quieter than 2026-08-24's print: `rvol` 1.184 (down from 1.935), +0.32% on the day, but the trailing week is now +7.41% and the month +14.27%, `up_down_ratio` 1.924, RSI14 73.04. Volume is lighter than the last note but price strength and participation are still lined up.

**AMD** (+4.91%, the day's largest move on the board) and **CMPS** (+4.79%) both advanced on volume below their own averages — `rvol` 0.733 and 0.817, `divergence` `mixed` and `weak` respectively. Gains without volume confirmation behind them.

**DGII**'s multi-session heavy-volume selloff has broken: `rvol` fell to 0.589 (from 1.586 last note), and it bounced +2.71% on the day. The weekly figure is still negative (-6.32%) but has improved sharply from -11.3%; still reads `mixed`.

Overall turnover was compressed relative to recent sessions — the day's highest `rvol` was JETS at 1.418, well below the 1.5-2.0 range that's topped the board on prior days. 20 of 42 symbols read `mixed`, 12 `quiet`, 8 `weak`, GLD alone `confirmed`, HE alone `washout`.

## Valuation

Cheap and rich ends of the board are unchanged from four sessions ago: **HE** (trailing P/E 8.814, `pe_percentile` 0.04, tracked universe) remains cheapest, followed by **FHB** (0.08), **MU** (0.091, within Technology), **SU** (0.12) and **BOH** (0.16) — same five names, same order, same percentiles. Richest: **IOT** (1.0, Technology, trailing P/E 391.4) and **TT** (1.0, Industrials, trailing P/E 33.95) still tied at the top of their peer groups, **TSLA** (0.96, tracked universe, trailing P/E 324.31) close behind.

Trailing-to-forward gaps are also stable, drifting only with price: **TSLA** 324.31 → 162.26 (the widest gap on the board), **ALAB** 139.24 → 44.21, **MCHP** 108.09 → 16.1, **AMD** 121.93 → 30.94, **MU** 21.08 → 6.02. Same five names carrying the size of gap as the last note; nothing has re-rated.

## Factors

`value_trap` is unchanged: **HE** remains the only name flagged true, watchlist-relative (cheapest print, `quality_score` 0.248, bottom-third). Against the S&P 500 Utilities benchmark (n=31), `bench_value_trap` is still false — `bench_quality_score` 0.4 sits mid-pack within-sector even though it's bottom-third against this watchlist. Same population disagreement as prior notes.

`reversal_tension` narrowed to eight names — **AAPL, FHB, HE, MCHP, MSFT, NXT, TSLA, TT** — down from ten. **AMD** and **BOH** both cleared: AMD's `mom_12_1` is now +195% against a `ret_1m` of only -3.19% (no longer enough tension to flag), and BOH's `ret_1m` eased to -4.82% against `mom_12_1` +21.1%.

Momentum and quality extremes are unchanged: **MU** still tops `mom_rank` (1.0, `mom_12_1` +666%, confirmed by `change_1y` +694%) and `quality_score` (0.922) watchlist-side, and `bench_mom_rank` (0.998) against the full S&P 500; **MSFT** still sits at `mom_rank` 0.0 despite the ongoing rally (month +26.6%), since the 12-1 window excludes the reversal-prone last month; **CMPS** still holds the lowest `quality_score` in either population (0.073 watchlist, 0.154 against the S&P 500). On within-sector quality, **NVDA** (`bench_quality_score` 0.942) still edges out MU (0.93) once measured against the full Technology sector.

## What changed since 2026-08-24

- **HE** graduated from `regime_verdict: confirmed` decline to today's first `washout` divergence verdict in this note series — a fourth session of losses, now on above-average volume.
- **GLD** held its `confirmed` verdict for a second session, on lighter volume than the prior print.
- **DGII**'s heavy-volume selloff broke: `rvol` down to 0.589 from 1.586, +2.71% bounce on the day.
- **AMD** and **BOH** cleared `reversal_tension`; no names newly flagged.
- Valuation percentiles at both ends of the distribution, and the `value_trap`/`bench_value_trap` split on HE, are unchanged.

## Hawaii tier

**HE** is the story here — see Volume above: first `washout` verdict in this series, RSI14 down to 26.63, still below both moving averages, `regime_verdict` confirmed, still the board's lone value-trap name. **FHB** eased -1.0% on the day, month now -6.35%, `rvol` 1.121 (`quiet`). **BOH** was today's second-highest `rvol` on the board (1.277) on a modest -0.9% day, `divergence` `quiet` — elevated turnover without a matching price move. **MATX** stayed flat (+0.06% day, month +8.66%), still holding the strongest Hawaii-tier momentum rank (0.8 watchlist, 0.95 against the S&P 500).

---
Tracker context, not trading advice. Nothing here is a recommendation to buy or sell any security.
