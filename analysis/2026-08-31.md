# 2026-08-31

GLD's `confirmed` divergence streak reached a sixth straight session — the longest run in this note series — while HE's `washout` streak matched it at five, and TSLA (back on the board since 8/28) posted the day's largest single-name move at +5.51%.

## Volume

**GLD** extended `confirmed` to a sixth consecutive session: day -0.12%, week -4.28% (down from +1.77% two notes ago), month +9.93%, `rvol` 0.806, `volume_trend` 1.321 (still expanding), `up_down_ratio` 1.604, RSI14 54.2. The heuristic's own detail reads "advance on expanding volume — participation backs the move," which is a reversal of the "confirming a drop" read on 8/28 — GLD's window turned positive again even though today's single-day print was essentially flat.

**HE** held `washout` for a fifth straight session: day -1.58%, week -1.67%, month -13.16% (marginally less negative than the -13.57% on 8/28, not a reversal), `rvol` 1.228, `volume_trend` 1.174 (still expanding), `up_down_ratio` 0.563, RSI14 26.67, still below both its 50- and 200-day averages. Detail: "decline on heavy volume — active selling, not drift."

**TSLA**, back on the board since 8/28, posted the day's largest single-name move (+5.51%, week +5.45%, month +18.23%) on `rvol` 1.843 — third-highest on the board — yet `divergence` reads only `weak` ("advance on contracting volume — the move is thinly supported"): the day's turnover was elevated, but the underlying `volume_trend` (0.824) is still contracting relative to the size of the move.

Two names carried the board's heaviest single-day turnover without the heuristic calling it significant: **RMIX** (`rvol` 2.622, the board's highest) fell -2.00% on what its trend-level `volume_trend` (0.733) still reads as "decline on light volume — drift rather than distribution" (`divergence` quiet), and **JETS** (`rvol` 1.863) fell -2.33% on the same quiet read (`volume_trend` 0.844). In both cases a single elevated day sits inside an otherwise below-average multi-week volume trend, which is why the verdict doesn't move.

**NXT** was the largest decliner among individual names by percentage after TSLA's gain, at -3.99% (`rvol` 1.198, `divergence` quiet — "decline on light volume").

Across the 42-symbol board the mix was quieter than the last note: 1 `confirmed` (GLD), 1 `washout` (HE), 9 `weak`, 8 `mixed`, 23 `quiet` — down from 12 `weak` / 11 `mixed` / 17 `quiet` on 8/28.

## Valuation

Cheap end reshuffled at the margin: **HE** (trailing P/E 8.698, `pe_percentile` 0.04, tracked universe), **FHB** (0.08, tracked universe), **NXT** (0.091, Technology — new to this list), **SU** (0.12, tracked universe), **BOH** (0.16, tracked universe). **MU**, which held the third spot at 0.091 (Technology) on 8/28, has moved out to 0.182 as its price has run; **NXT** takes its place after a -43.4% three-month slide pressured its multiple down within the Technology group.

Rich end is unchanged: **IOT** (1.0, Technology, trailing P/E 408.5) and **TT** (1.0, Industrials, trailing P/E 33.12) still tied at the top of their peer groups, with **TSLA** close behind at 0.96 (tracked universe, trailing P/E 340.69).

Widest trailing-to-forward P/E gaps: **IOT** 408.5 → 45.07, **TSLA** 340.69 → 170.46, **ALAB** 146.3 → 46.45, **MCHP** 108.02 → 16.09, **AMD** 120.39 → 30.47 — the same cluster as prior notes.

## Factors

`value_trap` is unchanged: **HE** remains the only name flagged true, watchlist-relative (cheapest print, `quality_score` 0.248, bottom-third). Against the S&P 500 Utilities benchmark (n=31), `bench_value_trap` is still false — `bench_quality_score` 0.4 sits mid-pack within-sector.

`reversal_tension` now reads **FHB, GOOG, HE, MSFT, NXT, TSLA** — six names, same count as 8/28 but not the same set: **META** cleared (`ret_1m` +2.81% no longer fights its still-negative `mom_12_1` of -25.64% — both point the same direction now), and **GOOG** is newly flagged (`mom_12_1` +68.42% against a `ret_1m` of -5.96%, the reversal-prone last month pulling against a strongly positive 12-1 formation return). **FHB, HE, MSFT, NXT, TSLA** are unchanged.

Momentum extremes are unchanged from 8/28: **MU** still tops both `mom_rank` (1.0) and `bench_mom_rank` (0.998, full S&P 500), `mom_12_1` now +576%. **META** sits at the bottom (`mom_rank` 0.0, `bench_mom_rank` 0.102) on a `mom_12_1` of -25.64%. Within Technology, `bench_quality_score` still has **MU** (0.93) narrowly ahead of **NVDA** (0.926), with watchlist-relative scores nearly tied (0.922 vs 0.921) — same read as the last note. **CMPS** still holds the lowest `quality_score` in either population (0.073 watchlist, 0.154 against the S&P 500).

## What changed since 2026-08-28

- **GLD** extended `confirmed` to a sixth straight session, now reading as an advance again rather than the decline flagged last note.
- **HE** held `washout` for a fifth straight session; month drawdown eased slightly to -13.16% from -13.57%.
- **TSLA** posted the board's largest single-day move (+5.51%) on elevated but trend-contracting volume (`divergence` weak).
- Cheap-end Technology reshuffled: **NXT** entered at `pe_percentile` 0.091, displacing **MU** (now 0.182) after NXT's three-month decline.
- `reversal_tension`: **META** cleared, **GOOG** newly flagged.
- The board's divergence mix turned quieter overall (`weak` 12→9, `mixed` 11→8, `quiet` 17→23).
- Rich-end valuation and momentum/quality extremes otherwise unchanged.

## Hawaii tier

**HE** is again the story — see Volume above: fifth straight `washout` session, RSI14 26.67, still below both moving averages, `regime_verdict` confirmed, still the board's lone value-trap name. **FHB** eased -0.70% on the day (week -1.65%, month -6.97%), `rvol` 0.91, `divergence` quiet, RSI14 28.44, still below both SMAs, and still carries `reversal_tension`. **BOH** fell -1.76% (week -1.20%, month -4.97%), a firmer down day than its recent flat prints, `rvol` 0.659, `divergence` quiet, RSI14 32.78. **MATX** advanced +0.22% (week +0.96%, month +11.17%) on `rvol` 1.002, `divergence` mixed, still holding the strongest Hawaii-tier momentum rank (0.8 watchlist, 0.954 against the S&P 500).

---
Tracker context, not trading advice. Nothing here is a recommendation to buy or sell any security.
