# 2026-08-27

NVDA posted the board's largest daily move (+8.74%) on by far its highest `rvol` (2.557) — yet its own `divergence` verdict reads only `mixed` — while GLD extended its `confirmed` streak to a fourth straight session, HE held `washout` for a third, and TSLA dropped off the board entirely after a data fetch failure.

## Volume

**NVDA** combined the day's largest single-name move (+8.74%) with the board's highest `rvol` at 2.557 — more than double the next-highest print (XYL at 1.3). Week is now +5.13%, month +19.98%, `up_down_ratio` 0.942, RSI14 61.32, still above both its 50- and 200-day averages. Despite the size of both the price move and the turnover behind it, `divergence` reads `mixed`, not `confirmed` — a reminder the heuristic weighs more than one day's numbers. Nothing in the data explains the move: next earnings isn't until 2026-11-17, 81 days out.

**GLD** extended `confirmed` to a fourth consecutive session — the longest streak in this note series — on lighter volume than the prior print (`rvol` 0.66, down from 0.88): +0.30% on the day, week +1.77%, month +13.88%, `up_down_ratio` 1.743, RSI14 66.68.

**HE** held `washout` for a third straight session: -0.26% on the day, week -1.29%, month -12.39%, `rvol` 0.757 (below its own average), `up_down_ratio` 0.557, RSI14 30.55, still below both its 50- and 200-day averages.

**IOT** (+5.10%) and **ALAB** (+4.75%) were the next-largest movers. IOT's turnover was modestly above its own average (`rvol` 1.111) though `volume_trend` is still contracting (0.793); ALAB advanced on volume below its own average again (`rvol` 0.623, `volume_trend` 0.829) — a gain without matching participation.

Across the 41-symbol board (TSLA absent, see below): 1 `confirmed` (GLD), 1 `washout` (HE), 11 `mixed`, 12 `weak`, 16 `quiet`.

## Valuation

Cheap end is unchanged from the last two notes: **HE** (trailing P/E 8.884, `pe_percentile` 0.042, tracked universe), **FHB** (0.083), **MU** (0.091, within Technology), **SU** (0.125), **BOH** (0.167) — same five names, same order, same percentiles.

Rich end: **IOT** (1.0, Technology, trailing P/E 418.70) and **TT** (1.0, Industrials, trailing P/E 33.80) still tied at the top of their peer groups. **TSLA**, which sat third at 0.96 in the prior two notes, is not in today's data at all — `meta.json` records `"TSLA": "no usable closes"` as the day's sole fetch failure, so its absence is a data gap, not a valuation change.

Widest trailing-to-forward P/E gaps: **IOT** 418.70 → 46.19, **ALAB** 142.77 → 47.56, **MCHP** 109.41 → 16.54, **AMD** 121.91 → 30.85 — the same cluster of names as prior notes, now led by IOT since TSLA (previously the single widest gap, 323.20 → 160.21) is off the board.

## Factors

`value_trap` is unchanged: **HE** remains the only name flagged true, watchlist-relative (cheapest print, `quality_score` 0.238, bottom-third). Against the S&P 500 Utilities benchmark (n=31), `bench_value_trap` is still false — `bench_quality_score` 0.4 sits mid-pack within-sector.

`reversal_tension` now reads **AAPL, AMZN, FHB, HE, MSFT** — five names, down from six in the last note, but not a clean net decline: **NXT** cleared (`ret_1m` eased to -3.3% against `mom_12_1` +35.1%, no longer enough tension), **AMZN** is newly flagged (`ret_1m` +13.06% pulling against a roughly flat `mom_12_1` of -0.9%), and **TSLA** — flagged in the prior note — is simply missing from today's board along with the rest of its data.

Momentum and quality extremes are largely unchanged: **MU** still tops `mom_rank` (1.0, `mom_12_1` +535%) and `bench_mom_rank` (1.0, full S&P 500); watchlist-relative `quality_score` is now tied at 0.918 between **MU** and **NVDA**, but within the Technology sector **NVDA** still edges ahead (`bench_quality_score` 0.942 vs MU's 0.93). **MSFT** sits near the bottom of `mom_rank` (0.042) despite a +29.57% month, since the 12-1 window excludes the reversal-prone last month. **CMPS** still holds the lowest `quality_score` in either population (0.076 watchlist, 0.154 against the S&P 500).

## What changed since 2026-08-26

- **TSLA** failed to fetch today (`"no usable closes"`) and dropped off the board entirely — the only fetch failure in `meta.json`. This removes it from the valuation rich-end and `reversal_tension` lists where it appeared in the last two notes; nothing about its standing actually changed, the data simply isn't there today.
- **NVDA** logged the largest single-day move (+8.74%) and highest `rvol` (2.557) seen in this note series, with `divergence` still reading `mixed` rather than `confirmed`.
- **GLD** extended `confirmed` to a fourth straight session, on lighter volume than the prior print.
- **HE** held `washout` for a third straight session.
- `reversal_tension`: **NXT** cleared, **AMZN** newly flagged.
- Cheap-end valuation percentiles are unchanged; the rich end is unchanged aside from TSLA's absence.

## Hawaii tier

**HE** is again the story — see Volume above: third straight `washout` session, RSI14 30.55, still below both moving averages, `regime_verdict` confirmed, still the board's lone value-trap name. **FHB** eased -0.54% on the day (week -2.97%, month -6.91%), `rvol` 0.952, `divergence` quiet, RSI14 30.37, still below both SMAs, and remains one of the five names carrying `reversal_tension`. **BOH** was essentially flat (-0.03% day), `rvol` 0.638, `divergence` quiet. **MATX** advanced +0.19% (week +2.45%, month +10.23%) on `rvol` 0.898, `divergence` mixed, still holding the strongest Hawaii-tier momentum rank (0.833 watchlist, 0.958 against the S&P 500).

---
Tracker context, not trading advice. Nothing here is a recommendation to buy or sell any security.
