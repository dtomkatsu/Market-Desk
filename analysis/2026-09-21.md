# 2026-09-21

The board's strongest breadth session since 9/17 (31 up, 11 down) was driven by three double-digit single-day gainers — **ALAB** +12.363%, **META** +11.428%, **AMD** +9.95% — even as **IOT** reversed hard into `washout` on its steepest single-day drop of this run (-3.793%).

## Volume

No name holds `confirmed` today, the fourth straight session at zero.

`washout` held at five names but membership turned over: **IOT** rejoined (see below) as **JETS** cleared to `quiet`. Continuing members: **GLD, HE, RMIX, XYL**.

**IOT** re-enters `washout` for the first time since its four-session `confirmed` streak broke to `quiet` on 9/18: day -3.793% (the board's largest single-day decline today), week -11.326%, month -2.386%, `rvol` 0.742 (below average — the decline is not itself on a volume spike), `volume_trend` 1.28 (expanding), `up_down_ratio` 0.99, RSI14 45.54. A clean reversal in character: the same name that confirmed an advance three weeks ago is now flagged on a decline against an expanding trend.

**HE** held `washout` for what looks like an eighth straight session: day -2.347%, week -7.625%, month -17.571% (deepening from 9/18's -16.311%), `rvol` 1.102, `volume_trend` 1.275 (still expanding), `up_down_ratio` 0.366 (more down-heavy than 9/18's 0.394), RSI14 15.72 — a new low for the run, down from 17.62 — `range_52w_position` 0.001, essentially sitting at a 52-week low. Still the board's lone `value_trap` name; more in Hawaii tier below.

**RMIX** held `washout` on an up day: day +1.89%, week +1.506%, month -23.756%, `rvol` 0.678, `volume_trend` 1.295 (still expanding), `up_down_ratio` 0.38 (still the board's most down-heavy reading), RSI14 29.36.

**XYL** (a held position) held `washout` for what looks like a sixth straight session: day -0.298%, week -0.233%, month -5.106%, `rvol` 0.922, `volume_trend` 1.231 (still expanding), `up_down_ratio` 0.662, RSI14 38.6, `range_52w_position` 0.04 — still near a fresh 52-week low.

**GLD** held `washout` on a down day: day -0.695%, week +1.41%, month -4.065%, `rvol` 0.552 (light), `volume_trend` 1.235 (still elevated), `up_down_ratio` 1.627 — the divergence here (up_down_ratio above 1 despite the `washout` verdict) is the same mismatch flagged in prior notes.

**JETS** cleared `washout` to `quiet`: day +2.799%, week +2.945%, month -0.854%, `rvol` 1.215, `volume_trend` 1.314 (still expanding), `up_down_ratio` 0.883 — an advance on expanding participation, resolving the up-day-on-a-down-label mismatch flagged for it on 9/16 and 9/17.

`weak` narrowed to six names from seven, with **ALAB** clearing despite today's board-leading gain: day +12.363%, `rvol` 1.695, `volume_trend` 0.856 (still contracting) — the move itself no longer reads as `weak`, though the underlying trend hasn't broadened. Holdovers: **AAPL** (day +0.848%, `rvol` 0.805, `volume_trend` 0.818, still contracting — another advance-on-contracting-trend session), **AMD** (day +9.95%, `rvol` 2.334 — the board's second-largest single-day reading — `volume_trend` 0.819, still contracting: a textbook advance-on-contracting-volume day), **CMPS** (day -1.185%, `rvol` 1.301, `volume_trend` 0.785), **MSFT** (day +1.586%, `rvol` 1.328, `volume_trend` 0.647 — still the board's lowest trend reading), **MU** (day +2.772%, `rvol` 1.134, `volume_trend` 0.678, still an advance-on-contracting-trend session, and still the board's top momentum name — see Factors), **XLK** (day +2.889%, `rvol` 1.392, `volume_trend` 0.834, contracting — the sector-ETF echo of the same tech-wide advance-on-contracting-participation pattern).

Across the 42-symbol board: 0 `confirmed`, 5 `washout` (GLD, HE, IOT, RMIX, XYL), 6 `weak` (AAPL, AMD, CMPS, MSFT, MU, XLK), 19 `mixed`, 12 `quiet`. Versus 9/18: `washout` unchanged in count (IOT in, JETS out), `weak` 7→6 (ALAB cleared), `mixed` 14→19, `quiet` 16→12.

Breadth: 31 up, 11 down, none flat — a sharp swing positive from 9/18's 19-up/23-down.

## Valuation

Cheap end (tracked universe) unchanged in order, no percentile crossings: **HE** (trailing P/E 7.465, up slightly from 9/18's 7.419, `pe_percentile` 0.04), **FHB** (10.948, down from 11.087, 0.08), **SU** (12.512, down from 12.568, 0.12), **BOH** (13.288, down from 13.535, 0.16). Within Technology (n=81), **NXT** remains the sector's cheapest name (trailing P/E 21.323, down from 21.486, `pe_percentile` 0.091, unchanged) and **MU** holds just above it (24.766, up from 23.571 on today's +2.772% move, `pe_percentile` 0.182, unchanged) — MU is also still the board's top momentum name (see Factors).

Rich end unchanged in membership: **IOT** (1.0, Technology, trailing P/E 255.067, up from 253.667), **TSLA** (1.0, tracked universe, 347.615, up from 344.312 on today's +3.028% move) and **TT** (1.0, Industrials, 32.6, up from 32.265) still tied at the top of their respective peer groups.

Widest trailing-to-forward P/E gaps: **IOT** 255.067 → 40.295 (gap 214.8, widening for the first time after six straight sessions of narrowing, from 213.6). **TSLA** 347.615 → 172.444 (gap 175.2, widened again from 173.5). **ALAB** 178.167 → 56.849 (gap 121.3, widened sharply from 114.6 on today's +12.363% move) — enough to **swap past AMD** for third place, the first order change in this gap ranking in several sessions. **AMD** 158.317 → 40.061 (gap 118.3, widened from 117.5). **MCHP** 111.5 → 16.542 (gap 95.0, widened from 93.6). **DGII** 59.397 → 23.895 (gap 35.5, widened from 34.5). **HLIT** 47.042 → 13.978 (gap 33.1, essentially flat, from 33.2). **MU** 24.766 → 6.897 (gap 17.9, widened from 17.0).

## Factors

`value_trap` is unchanged: **HE** remains the only name flagged, watchlist-relative (`value_score` 0.978, `quality_score` 0.248, bottom-third). Against the S&P 500 Utilities benchmark (n=31), `bench_value_trap` is still false — `bench_value_score` 0.983, `bench_quality_score` 0.4, unchanged, mid-pack within-sector. A trap flag reads as caution, not a case for either side of a trade.

`reversal_tension` shifted membership while holding at six-to-seven names: **GHRS** (`mom_12_1` +116.944% vs `ret_1m` -2.615%), **DGII** (+93.151% vs -1.182%) and **TT** (+12.603% vs -3.714%) cleared, while **BOH** (`mom_12_1` +17.452% vs `ret_1m` -5.29%, newly flagged) and **TSLA** (-17.205% vs +8.742%, re-flagged after clearing on 9/18) joined. **HLIT, LLY, META, NXT** carry the flag unchanged.

Momentum extremes are unchanged in ranking: **MU** still tops both `mom_rank` (1.0) and `bench_mom_rank` (1.0, full S&P 500 cross-section), with `mom_12_1` continuing to ease, now +477.8% (from +486.7% on 9/18), extending the pullback from 9/16's peak of +542.3%. **META** remains at the bottom (`mom_rank` 0.0, `bench_mom_rank` 0.062), `mom_12_1` -29.8% (a touch more negative than 9/18's -29.4%) against a `ret_1m` that jumped to +35.9% (from +21.9%) on today's +11.428% single-day move — the widest 12-1-vs-recent-month gap on the board, though META does not carry the `reversal_tension` flag today. Quality extremes are unchanged, scores identical to 9/18 since these are fundamentals-driven: **MU** (0.922 watchlist, 0.927 within Technology) narrowly ahead of **NVDA** (0.921 / 0.926); **CMPS** still lowest in both populations (0.073 watchlist, 0.15 vs the S&P 500).

## What changed since 2026-09-18

- Breadth swung sharply positive: 31 up, 11 down, versus 9/18's 19-up/23-down.
- **IOT** re-entered `washout` on its steepest single-day decline of the run (-3.793%), less than a week after its four-session `confirmed` streak broke; **JETS** cleared `washout` to `quiet`.
- **ALAB** cleared `weak` despite posting the board's largest single-day gain (+12.363%); `weak` narrowed to six names from seven.
- **HE** extended `washout` further, RSI14 down to a fresh streak-low of 15.72 and `range_52w_position` essentially at zero.
- Cheap and rich valuation ends held their order with no percentile crossings; the trailing-to-forward P/E gap ranking saw its first order change in several sessions as **ALAB**'s gap widened past **AMD**'s.
- `reversal_tension` turned over: **GHRS, DGII, TT** cleared; **BOH** and **TSLA** newly (re-)flagged; **HLIT, LLY, META, NXT** unchanged.
- **MU**'s `mom_12_1` continued easing (+486.7% → +477.8%); **META**'s `ret_1m` jumped to +35.9% from +21.9% on today's move, widening its 12-1-vs-recent-month gap further.
- `value_trap` and quality extremes are otherwise unchanged from 9/18.

## Hawaii tier

**HE** is the story again — see Volume above: `washout` for what looks like an eighth straight session, day -2.347%, week -7.625%, month -17.571% (deepening from -16.311%), `up_down_ratio` 0.366, `volume_trend` still expanding at 1.275, RSI14 15.72 (a new streak low), `range_52w_position` 0.001, still the board's lone `value_trap` name, `reversal_tension` still false (both `mom_12_1` -4.68% and `ret_1m` -17.571% negative — signal and recent month agree). **BOH** fell -2.438% (week -4.184%, month -5.29%), `rvol` 1.927, `divergence` quiet, RSI14 27.87 — down sharply from 9/18's 35.61 and now oversold — still below both its 50- and 200-day averages, and newly carries `reversal_tension` (`mom_12_1` +17.452% vs `ret_1m` -5.29%, see Factors). **FHB** fell -1.009% (week -1.086%, month -4.171%), `rvol` 1.248, `divergence` mixed, RSI14 37.18, still below both SMAs, `reversal_tension` still false. **MATX** rose +0.773% (week -0.803%, month +8.281%), `rvol` 1.017, `divergence` unchanged at mixed, RSI14 64.36 (up slightly from 62.44), `range_52w_position` 0.96, still near a 52-week high. Still holding the strongest Hawaii-tier momentum rank (0.84 watchlist, 0.964 against the S&P 500, unchanged).

---
Tracker context, not trading advice. Nothing here is a recommendation to buy or sell any security.
