# 2026-09-03

The board had its broadest and quietest up day in this note series — 35 of 42 names higher and nothing moved more than 6.4% — while IOT printed the highest single-day `rvol` (3.192) seen anywhere in this series on a +5.33% gain that the divergence heuristic still reads as `quiet`, and HE broke its run of down days with a +1.28% print even as its `washout` divergence read held.

## Volume

**IOT** traded 3.192x its 20-day average volume — the highest `rvol` in this note series, surpassing RMIX's 3.781 print on 9/1 only in relative terms for today's board — on a +5.33% day, yet `divergence` reads `quiet`: `volume_trend` sits at 0.985, just under the expansion threshold, so a single very heavy day inside an otherwise average multi-week trend doesn't move the verdict. Same disconnect as RMIX and JETS in prior notes, now on the day's biggest-volume name.

**SU** (Suncor, held position) traded at 3.126x average volume on a session that was essentially flat (-0.22%); `divergence` reads `mixed`, `volume_trend` 0.927.

**HE** held `washout` again today, but the underlying pattern flipped: today's print was positive (+1.28%, the first up day for HE in this note series), yet the heuristic still reads `washout` — `up_down_ratio` 0.508 and `volume_trend` 1.229 (still expanding) reflect a multi-day window that remains net down-and-heavy even as today's single bar rose. Week -3.23%, month -11.91%, RSI14 28.72, still below both its 50- and 200-day averages.

**GLD** reads `confirmed` again (it ran seven straight confirmed sessions through the 9/1 note; no note was published for 9/2, so this note can't say whether that streak held through today without a gap). Today's move is a gain on expanding volume — `rvol` 1.017, `volume_trend` 1.34, `up_down_ratio` 1.927 — the same "advance, participation backs it" pattern flagged on 8/31, not 9/1's "decline on heavy volume" read.

**TSLA** posted the board's second-largest gain (+5.42%) on `rvol` 1.806, but `divergence` reads only `mixed`: `volume_trend` (0.894) is still below its expansion threshold, so the move isn't confirmed by trend-level turnover. **SPCX** was the day's single largest mover (+6.42%) on `rvol` 1.158, `divergence` quiet; this name still has too little price history for a momentum, regime or volatility read (`mom_12_1`, `annualized_vol`, `regime_percentile` all not reported).

Across the 42-symbol board: 1 `confirmed` (GLD), 1 `washout` (HE), 5 `weak` (AAPL, MATX, MSFT, MU, XLV), 11 `mixed` (DGII, GHRS, JETS, LLY, META, NVDA, RMIX, SU, TSLA, XLE, XYL), 24 `quiet` — `weak` down from six on 9/1, `mixed` up from ten, `quiet` unchanged at 24.

## Valuation

Cheap end has flip-flopped again: **HE** (trailing P/E 8.597, `pe_percentile` 0.04, tracked universe), **FHB** (0.08, tracked universe), **NXT** (0.091, Technology), **SU** (0.12, tracked universe), **BOH** (0.16, tracked universe). **NXT** is back in the third spot and **MU** back out to 0.182 (Technology) — the same swap the 8/31 note flagged, undone on 9/1, and now back again. Two round trips between these two names inside a week is worth naming as a pattern in itself: both sit near the bottom of Technology's multiple distribution and keep trading places at the margin as their prices move.

Rich end is unchanged: **IOT** (1.0, Technology, trailing P/E 387.5) and **TT** (1.0, Industrials, trailing P/E 33.15) still tied at the top of their peer groups, **TSLA** close behind at 0.96 (tracked universe, trailing P/E 351.74, up from 332.79 on 9/1 after today's gain).

Widest trailing-to-forward P/E gaps, same cluster as prior notes: **IOT** 387.5 → 42.75, **TSLA** 351.74 → 174.36, **ALAB** 139.32 → 44.24, **AMD** 116.07 → 29.52, **MCHP** 107.52 → 16.02.

## Factors

`value_trap` is unchanged: **HE** remains the only name flagged true, watchlist-relative (cheapest print, `quality_score` 0.248, bottom-third). Against the S&P 500 Utilities benchmark (n=31), `bench_value_trap` is still false — `bench_quality_score` 0.4 sits mid-pack within-sector. No name on the board carries a true `bench_value_trap` flag.

`reversal_tension` widened to ten names — **ALAB, AMD, AMZN, FHB, GOOG, HE, MCHP, NXT, TSLA, TT** — up from eight on 9/1. **BOH** cleared (last month's decline eased to -2.88% from -5.34%, no longer fighting its positive 12-1 formation return hard enough to flag); **HE** (`mom_12_1` +1.29% vs `ret_1m` -11.91%), **MCHP** (+25.4% vs -5.45%) and **TT** (+19.26% vs -7.94%) are newly flagged. **ALAB, AMD, AMZN, FHB, GOOG, NXT, TSLA** carry the flag unchanged.

Momentum extremes are unchanged from 9/1: **MU** still tops both `mom_rank` (1.0) and `bench_mom_rank` (0.998, full S&P 500), `mom_12_1` now +654%. **META** sits at the bottom (`mom_rank` 0.0, `bench_mom_rank` 0.12) on a `mom_12_1` of -19.86%. Within Technology, `bench_quality_score` still has **MU** (0.927) narrowly ahead of **NVDA** (0.926), with watchlist-relative scores nearly tied (0.922 vs 0.921) — identical to the 9/1 read. **CMPS** still holds the lowest `quality_score` in either population (0.073 watchlist, 0.154 against the S&P 500).

## What changed since 2026-09-01

- A broad but shallow rally reversed 9/1's broad sell-off: 35 of 42 names higher, the largest move on the board only +6.42% (SPCX).
- **HE** printed its first up day in this note series (+1.28%) while `divergence` stayed `washout` on multi-day metrics (`up_down_ratio` 0.508, `volume_trend` still expanding).
- **IOT** posted the highest single-day `rvol` (3.192) seen in this series, still read as `quiet` on a volume trend just under its expansion threshold.
- Cheap-end Technology swapped again: **NXT** back to `pe_percentile` 0.091, **MU** back out to 0.182 — the third flip between the two names across the last three notes.
- `reversal_tension` widened from eight to ten names: **BOH** cleared; **HE, MCHP, TT** newly flagged.
- Divergence mix shifted: `weak` 6→5, `mixed` 10→11, `quiet` unchanged at 24 (`confirmed`/`washout` unchanged at one each).
- Rich-end valuation and momentum/quality extremes otherwise unchanged.

## Hawaii tier

**HE** is again the story — see Volume above: first up day (+1.28%) in this note series but `divergence` still `washout` on multi-day turnover, RSI14 28.72, still below both moving averages, `regime_verdict` confirmed, still the board's lone value-trap name, and newly carrying `reversal_tension` again. **FHB** rose +0.97% (week +0.70%, month -5.49%), `rvol` 0.963, `divergence` quiet, RSI14 40.07, still below both SMAs, still carrying `reversal_tension`. **BOH** rose +0.18% (week +0.16%, month -2.88%), `rvol` 0.96, `divergence` quiet, RSI14 43.31, and cleared `reversal_tension` today as its trailing month's decline eased. **MATX** advanced +1.86% (week +0.09%, month +8.36%) on `rvol` 1.184, `divergence` weak (volume trend below its expansion threshold), still holding the strongest Hawaii-tier momentum rank (0.8 watchlist, 0.944 against the S&P 500).

---
Tracker context, not trading advice. Nothing here is a recommendation to buy or sell any security.
