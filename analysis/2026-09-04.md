# 2026-09-04

IOT traded 4.542x its 20-day average volume today — the highest single-day `rvol` printed anywhere in this note series, surpassing 9/3's 3.192 — on a +3.74% day that `divergence` still reads as only `quiet`.

## Volume

**IOT** posted that 4.542x `rvol` (new series high) on a +3.74% gain, yet `divergence` reads `quiet`: `volume_trend` is 1.091, `up_down_ratio` 1.852 — one very heavy day sitting inside a multi-week trend the heuristic doesn't treat as expanding enough to confirm.

**ALAB** had the board's largest single-day move, +9.75%, on `rvol` 1.941 — but `volume_trend` is 0.696 (contracting) and `divergence` reads `quiet`: an advance on contracting trend-level volume, the "gain not backed by participation" pattern, despite the size of the price move. `up_down_ratio` 0.779.

**TSLA** had the board's largest decline, -5.92%, on `rvol` 1.76; `volume_trend` is 0.92 (also contracting), and `divergence` reads `mixed` rather than a clean confirmed drop.

**HE** closed exactly flat (11.09 → 11.09, `change_1d` 0.0%) after four straight lower closes, but `divergence` holds at `washout`: `up_down_ratio` 0.526, `volume_trend` 1.222 (still expanding), RSI14 28.72, still below both its 50- and 200-day averages. More in Hawaii tier below.

**GLD** reads `confirmed` again: day -0.84%, week -0.52%, `volume_trend` 1.33 (expanding), `up_down_ratio` 1.758 — a decline on expanding volume, the confirming-a-drop pattern rather than the advancing read seen on some earlier sessions.

**SU** (held position) traded at `rvol` 1.248 on a -1.33% day, `divergence` mixed.

Across the 42-symbol board the day was close to an even split — 20 higher, 21 lower, HE flat — a much narrower session than 9/3's 35-up rally. Divergence mix: 1 `confirmed` (GLD), 1 `washout` (HE), 4 `weak`, 13 `mixed`, 23 `quiet` — `mixed` up from 11 on 9/3, `weak` down from 5, `quiet` down from 24, `confirmed`/`washout` unchanged at one each.

## Valuation

Cheap end is unchanged in both order and value from 9/3: **HE** (trailing P/E 8.597, `pe_percentile` 0.04, tracked universe), **FHB** (0.08, tracked universe), **NXT** (0.091, Technology), **SU** (0.12, tracked universe), **BOH** (0.16, tracked universe), **MU** (0.182, Technology) — a quiet day at the bottom of the board.

At the top, **TSLA** moved to `pe_percentile` 1.0 within the tracked-universe group, up from 0.96 on 9/3, joining **IOT** (1.0, Technology) and **TT** (1.0, Industrials) at the top of their respective peer groups — three different populations, each showing its own name at the ceiling. TSLA's own trailing P/E actually fell today, 351.74 → 324.844, on the day's decline; it moved up the percentile because **IOT's** trailing P/E fell even harder, from 387.5 (9/3 note) to 268.0. That drop traces to `trailing_eps`: back-calculating from 9/3's reported multiple gives roughly 0.10, against 0.15 today — a new trailing-twelve-month earnings figure rolling in, not a re-rating of the stock.

Widest trailing-to-forward P/E gaps: **MCHP** 109.074 → 16.247, **IOT** 268.0 → 42.612 (narrower than 9/3's 387.5 → 42.75 given the trailing-P/E move above), **AMD** 121.829 → 30.909, **MU** 22.963 → 6.558, **HLIT** 49.083 → 14.584, **ALAB** 152.157 → 48.549. **TSLA**, despite carrying the board's highest raw trailing multiple, has one of the narrower trailing/forward gaps (324.844 → 164.034) — its two multiples are closer together than IOT's or MCHP's.

## Factors

`value_trap` is unchanged: **HE** remains the only name flagged, watchlist-relative (`value_score` 0.978, `quality_score` 0.248, bottom-third). Against the S&P 500 Utilities benchmark (n=31), `bench_value_trap` is still false — `bench_quality_score` 0.4 sits mid-pack within-sector.

`reversal_tension` narrowed to nine names from ten on 9/3. **AMD**, **FHB** and **MCHP** cleared — MCHP's trailing month turned positive (`ret_1m` +0.35%, vs a negative print on 9/3), AMD's and FHB's declines eased to -2.39% and -4.54%. **DGII** (`mom_12_1` +143.4% vs `ret_1m` -16.4%) and **GHRS** (`mom_12_1` +118.1% vs `ret_1m` -7.9%) are newly flagged. **ALAB, AMZN, GOOG, HE, NXT, TSLA, TT** carry the flag unchanged.

Momentum extremes are unchanged: **MU** still tops both `mom_rank` (1.0) and `bench_mom_rank` (0.998, full S&P 500 cross-section), though its `mom_12_1` eased to +610.8% from +654% on 9/3. **META** remains at the bottom (`mom_rank` 0.0, `bench_mom_rank` 0.122), `mom_12_1` -20.9%. Quality extremes also unchanged: **MU** (0.922 watchlist, 0.927 within Technology) narrowly ahead of **NVDA** (0.921 / 0.926); **CMPS** still lowest in both populations (0.073 watchlist, 0.154 vs the S&P 500).

## What changed since 2026-09-03

- A roughly even session (20 up, 21 down, HE flat) followed 9/3's broad 35-up rally.
- **IOT** printed the highest single-day `rvol` seen in this note series (4.542), beating 9/3's 3.192, still read `quiet`.
- **ALAB** posted the board's largest single-day gain (+9.75%) on rising `rvol` but a contracting `volume_trend` — an advance not backed by trend-level participation.
- **TSLA** joined **IOT** and **TT** at `pe_percentile` 1.0 in its own peer group, up from 0.96, as **IOT's** trailing P/E fell sharply (387.5 → 268.0) on an EPS update rather than a price move.
- `reversal_tension` narrowed from ten names to nine: **AMD, FHB, MCHP** cleared; **DGII, GHRS** newly flagged.
- Divergence mix shifted: `mixed` 11→13, `weak` 5→4, `quiet` 24→23 (`confirmed`/`washout` unchanged at one each).
- Cheap-end valuation and quality extremes otherwise unchanged.

## Hawaii tier

**HE** closed flat (11.09, `change_1d` 0.0%) — its first non-negative print in several sessions — but `divergence` stays `washout` on multi-day metrics (`up_down_ratio` 0.526, `volume_trend` still expanding at 1.222); RSI14 28.72, still below both moving averages, still the board's only `value_trap` name, and `reversal_tension` continues (`mom_12_1` +0.97% vs `ret_1m` -11.2%). **FHB** rose +0.27% (week +1.09%, month -4.54%), `rvol` 0.775, `divergence` quiet, RSI14 41.29, still below both SMAs, and cleared `reversal_tension` today. **BOH** rose +0.71% (week +0.78%, month -1.42%), `rvol` 0.774, `divergence` quiet, RSI14 46.94, above its 200-day but not its 50-day average. **MATX** rose +1.49% (week +0.57%, month +9.37%) on `rvol` 0.973, `divergence` weak, still holding the strongest Hawaii-tier momentum rank (0.8 watchlist, 0.946 against the S&P 500).

---
Tracker context, not trading advice. Nothing here is a recommendation to buy or sell any security.
