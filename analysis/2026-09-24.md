# 2026-09-24

Today's highest single reading on the board is **GHRS**'s `rvol` of 2.324 on a real -5.817% day (RSI14 down to 30.58) — yet the divergence heuristic tags it only `quiet`, a genuine decline-on-heavy-volume session the 20-day-trend heuristic misses.

## Volume

`confirmed` sits at zero for a fifth straight session (9/18, 9/21, 9/22, 9/23, 9/24).

`washout` narrowed to four names from five: **IOT** cleared to `quiet`. Continuing: **HE, JETS, RMIX, XYL**.

**HE**: day -0.542% (much milder than 9/23's -3.657%), week -9.118%, month -19.349% (still deepening), `rvol` 1.327, `volume_trend` 1.32 (still expanding), `up_down_ratio` 0.367, RSI14 14.63 (a touch off 9/23's fresh low of 13.37, still deeply oversold), `regime_verdict` still **confirmed** — the board's clearest sustained decline-on-expanding-volume read.

**RMIX**: day -2.671%, week -6.982%, month -28.34%, `rvol` 1.545 (up from 9/23's light 0.609 — now genuinely heavy), `volume_trend` 1.353, `up_down_ratio` 0.328 (the board's most down-heavy reading), RSI14 23.7. A real heavy-volume decline today, not just a trend-window artifact.

**XYL**: day -5.504% (reversing 9/23's +1.305% up day), week -4.591%, month -8.651%, `rvol` 1.584, `volume_trend` 1.274, `up_down_ratio` 0.688, RSI14 30.44, `regime` turbulent. The mismatch flagged in the last note — `washout` printed on an up day — has resolved: today is a genuine decline on heavy volume.

**JETS**: day +0.743% (up), week +1.029%, month -5.417%, `rvol` 0.917 (light, down from 9/23's 1.909), `volume_trend` 1.365 (still the board's most expanding 20-day reading), `up_down_ratio` 0.949, RSI14 45.07 — the same up-day-tagged-`washout` mismatch now sitting on JETS instead of XYL.

**GHRS** (not tagged `washout`): day -5.817%, week -14.868%, month -15.456%, `rvol` 2.324 — the single highest reading on today's board — `volume_trend` 0.725 (contracting), `up_down_ratio` 1.346, RSI14 30.58. A genuine decline on heavy single-day volume that the 20-day-trend/up-down-ratio heuristic reads as `quiet` because the trailing window still leans up-day-heavy.

`weak` narrowed to one name from two: **XLK** cleared to `mixed`. Remaining: **MU** — day +0.807% (advance), week +10.54%, month +15.816%, `rvol` 0.875, `volume_trend` 0.723 (the board's lowest, i.e. most contracting), `up_down_ratio` 0.959, RSI14 63.02 — an advance on contracting volume, the cleanest example of that pattern on the board today.

Across the 42-symbol board: 0 `confirmed`, 4 `washout`, 1 `weak`, 23 `mixed`, 14 `quiet`. Breadth: 17 up, 25 down, none flat — still net negative but well off 9/23's 12 up/30 down. Heaviest `rvol`: **GHRS** (2.324), then **BOH** (1.884) and **TLT** (1.81). Lightest: **AAPL** (0.569) and **NVDA** (0.589).

## Valuation

Cheap end (tracked universe, n=11) held its exact order and percentiles from 9/23: **HE** (trailing P/E 7.109, `pe_percentile` 0.04), **FHB** (10.978, 0.08), **SU** (12.794, 0.12), **BOH** (13.318, 0.16). Within Technology (n=11), **NXT** remains cheapest (20.891, `pe_percentile` 0.091) just ahead of **MU** (24.492, 0.182) — unchanged from 9/23.

Rich end unchanged in membership and order, all still at the top of their groups, though two of the three actually got cheaper on lower prices: **IOT** (1.0, Technology, trailing P/E 253.067, down from 9/23's 262.133), **TSLA** (1.0, tracked universe, 338.282, down from 356.547) and **TT** (1.0, Industrials, 33.863, up slightly from 32.678).

Widest trailing-to-forward P/E gaps, order unchanged from 9/23: **IOT** 253.067 → 39.979 (gap 213.1). **TSLA** 338.282 → 171.251 (gap 167.0, narrowed from 184.5). **ALAB** 180.505 → 57.03 (gap 123.5). **AMD** 161.7 → 40.501 (gap 121.2). **MCHP** 114.043 → 17.168 (gap 96.9, widened from 93.5). **DGII** 62.143 → 25.0 (gap 37.1). **HLIT** 45.875 → 13.631 (gap 32.2). **MU** 24.492 → 6.786 (gap 17.7).

## Factors

`value_trap` unchanged: **HE** remains the only name flagged, watchlist-relative (`value_score` 0.978, `quality_score` 0.248, bottom-third). Against the S&P 500 Utilities benchmark (n=31), `bench_value_trap` is still false (`bench_value_score` 0.983, `bench_quality_score` 0.4). A trap flag is caution, not a case for either side.

`reversal_tension` membership turned over while holding at eight names: **FHB** and **LLY** cleared; **CMPS** (`mom_12_1` +195.238% vs `ret_1m` -12.03%) and **COST** (+2.333% vs -6.618%) are newly flagged. Continuing: **BOH** (+18.638% vs -5.751%), **GHRS** (+111.868% vs -15.456%), **HLIT** (+21.161% vs -11.048%), **META** (-24.34% vs +36.515%), **NXT** (+20.196% vs -5.707%), **TSLA** (-17.753% vs +7.906%, narrower than 9/23's -19.636%/+8.933%). This flag reads as "last month fought the 12-1 signal," not a signal itself.

Momentum: **MU** still tops both `mom_rank` (1.0) and `bench_mom_rank` (0.998, full S&P 500 cross-section, up from 9/23's 0.996); its `mom_12_1` ticked back up to +461.545% from 9/23's +453.937%, a partial rebound after two sessions of pullback. **META** remains at the bottom (`mom_rank` 0.0, `bench_mom_rank` 0.082), `mom_12_1` -24.34% — continuing to firm from 9/22's -29.122% and 9/23's -26.751%. Quality extremes unchanged: **MU** (0.922 watchlist, 0.927 within Technology, n=81) narrowly ahead of **NVDA** (0.921 / 0.926); **CMPS** still lowest in both populations (0.073 watchlist, 0.15 within the S&P 500, n=503) — CMPS's `value_score` and `pe_percentile` are both null (trailing and forward P/E null on negative earnings), so no valuation percentile is reported for it; that is a genuine gap, not a zero.

## What changed since 2026-09-23

- Breadth improved but stayed negative: 17 up / 25 down, versus 9/23's 12 up / 30 down.
- `washout` narrowed from five to four as **IOT** cleared to `quiet`; **HE, JETS, RMIX, XYL** held, though the mismatch member shifted from XYL (now a genuine heavy-volume decline) to JETS (now an up day tagged `washout`).
- **GHRS** posted the board's heaviest single-day `rvol` (2.324) on a real -5.817% decline, but the heuristic still reads it as `quiet`.
- `weak` narrowed from two to one as **XLK** cleared to `mixed`; **MU** remains, still the board's most contracting `volume_trend`.
- `confirmed` stayed at zero for a fifth straight session.
- `reversal_tension` held at eight names but turned over: **FHB** and **LLY** cleared; **CMPS** and **COST** newly flagged.
- **MU**'s `mom_12_1` rebounded (+453.937% → +461.545%) and **META**'s continued to firm (-26.751% → -24.34%).
- Cheap-end valuation order and percentiles held exactly; the rich end's **IOT** and **TSLA** both got modestly cheaper on lower prices while **TT** ticked up slightly.
- `value_trap` and quality extremes are otherwise unchanged.

## Hawaii tier

**BOH** and **FHB** both bounced after 9/23's rout: BOH +1.123% (week -4.626%, month -5.751%), `rvol` 1.884 (second-highest on the board), RSI14 recovering to 27.38 from 9/23's streak-low 21.13, still `reversal_tension`-flagged; FHB +1.372% (week -2.673%, month -2.56%), `rvol` 0.945, RSI14 36.32, and its `reversal_tension` flag cleared today. **HE** extended its decline again but on a much smaller day (-0.542% vs 9/23's -3.657%), RSI14 still in the mid-teens (14.63), `washout` and `regime_verdict` **confirmed** both holding, and still the tier's only `value_trap`. **MATX** ticked up +1.311% (week -4.23%, month +0.898%) on a `quiet` divergence read — no unusual participation behind the move — while holding the tier's strongest momentum standing (`mom_rank` 0.88, `bench_mom_rank` 0.97 against the full S&P 500).

---
Tracker context, not trading advice. Nothing here is a recommendation to buy or sell any security.
