# 2026-09-22

The most notable thing on today's board isn't a price move: exactly half the 42 tracked symbols — including all four Hawaii-tier names, all five held positions and every current `washout`/most `weak` members except MSFT and MU — carry no new session and are still printing 2026-09-21's close, so their day/week/month figures and divergence verdicts are unchanged repeats of the last note rather than new information.

## Data note

18 of 42 symbols show `last_date` 2026-09-22 (a genuine new bar): AAPL, ALAB, AMD, AMZN, GLD, GOOG, IWM, META, MSFT, MU, NVDA, QQQ, SPCX, SPY, TLT, TSLA, XLE, XLF. The other 24 — including **BOH, FHB, HE, MATX** (the whole Hawaii tier) and **CMPS, NXT, SU, TT, XYL** (the whole Holdings tier) — still show `last_date` 2026-09-21, i.e. no new candle. Their price, volume and momentum figures below are therefore identical to what the 9/21 note already reported, not a new read; a few of their trailing/forward P/E figures shifted slightly on revised fundamentals data even though the underlying price didn't move. Everything discussed as "today's move" below is drawn only from the 18 fresh names.

## Volume

No name holds `confirmed` — zero for a third straight session (9/18, 9/21, 9/22).

`washout` held at the same five names as 9/21 — **GLD, HE, IOT, RMIX, XYL** — but only **GLD** has a fresh bar; the other four are unchanged repeats of 9/21's numbers (already covered in the last note). **GLD** (fresh): day +0.424%, week +1.502%, month -5.501%, `rvol` 0.582 (light), `volume_trend` 1.165 (still heavy on the 20-day window), `up_down_ratio` 1.73, RSI14 49.88 — the same up-day-on-a-`washout`-label mismatch flagged in prior notes, now on genuinely new data: more up days than down over the window, yet the 20-day trend still reads as a heavy decline.

`weak` narrowed to four names from six, and the change is real: **AAPL** and **AMD** both cleared on fresh bars. **AAPL**: day +0.227%, `volume_trend` rose to 0.886 from 9/21's 0.818, just crossing above the 0.85 "light" threshold — the advance itself hasn't changed, its volume trend classification has. **AMD**: day +1.34%, `volume_trend` rose to 0.856 from 0.819, the same threshold-crossing. Remaining `weak`: **MSFT** (fresh: day -0.72%, week +0.177%, month +3.054%, `rvol` 1.015, `volume_trend` 0.713, up from 9/21's 0.647 but still the board's most contracting reading), **MU** (fresh: day +5.0%, week +18.172%, month +13.383%, `rvol` 1.16, `volume_trend` 0.695, up from 0.678 — still an advance-on-contracting-trend session despite the board's largest fresh-data gain today), **CMPS** and **XLK** (both stale, unchanged from 9/21).

**XLF** stands out separately: `rvol` 2.262 — the board's largest single-day volume reading today — on a -1.968% decline (week -3.265%, month -4.325%), yet its 20-day `volume_trend` is only 1.014, below the 1.15 "heavy" bar, so the divergence heuristic still reads it as `mixed` rather than `washout`. A real single-day volume spike that the 20-day-window heuristic doesn't (yet) register.

Across the 42-symbol board: 0 `confirmed`, 5 `washout`, 4 `weak`, 20 `mixed`, 13 `quiet`. Raw breadth is 26 up / 16 down, but that blends 16 up / 8 down from the 24 stale names (recycled 9/21 figures) with 10 up / 8 down from the 18 names with a genuine 9/22 print — the fresh-only read is close to flat, mildly positive.

## Valuation

Cheap end (tracked universe, n=25) held its order with no percentile crossings, all four stale: **HE** (trailing P/E 7.092, `pe_percentile` 0.04), **FHB** (10.774, 0.08), **SU** (12.707, 0.12), **BOH** (13.322, 0.16). Within Technology (tracked universe, n=11), **NXT** remains cheapest (21.14, `pe_percentile` 0.091, stale) and **MU** holds just above it — fresh today at 24.207, `pe_percentile` 0.182 unchanged.

Rich end unchanged in membership, all effectively stale-priced: **IOT** (1.0, Technology n=11, trailing P/E 260.533), **TSLA** (1.0, tracked universe n=25, 348.734, the one fresh print among the three) and **TT** (1.0, Industrials n=3, 32.522) still tied at the top of their respective peer groups.

Widest trailing-to-forward P/E gaps, order unchanged: **IOT** 260.533 → 41.159 (gap 219.4). **TSLA** 348.734 → 172.999 (gap 175.7). **ALAB** 176.696 → 56.379 (gap 120.3). **AMD** 154.038 → 39.472 (gap 114.6). **MCHP** 109.464 → 16.478 (gap 93.0). **DGII** 57.923 → 24.042 (gap 33.9). **HLIT** 45.792 → 13.606 (gap 32.2). **MU** 24.207 → 6.736 (gap 17.5). No order swaps; several of the stale-priced names' gaps moved a little on revised fundamentals rather than a new close.

## Factors

`value_trap` unchanged: **HE** remains the only name flagged, watchlist-relative (`value_score` 0.978, `quality_score` 0.248, bottom-third), stale. Against the S&P 500 Utilities benchmark (n=31), `bench_value_trap` is still false (`bench_value_score` 0.983, `bench_quality_score` 0.4). A trap flag is caution, not a case for either side.

`reversal_tension` narrowed to five names from six: **TSLA** cleared on fresh data — `mom_12_1` -14.836% vs `ret_1m` +4.42%, a much narrower gap than 9/21's -17.205%/+8.742%. **BOH, HLIT, LLY, NXT** carry the flag unchanged (all stale). **META** also still carries it (fresh: `mom_12_1` -29.122%, `ret_1m` +34.057%, still the board's widest 12-1-vs-recent-month gap, though a touch narrower than 9/21's -29.8%/+35.9%).

Momentum: **MU** still tops both `mom_rank` (1.0) and `bench_mom_rank` (0.998, full S&P 500 cross-section), and on fresh data its `mom_12_1` actually rose to +495.054% from 9/21's +477.8% — reversing, for now, the pullback that had been running since 9/16's +542.3% peak. **META** remains at the bottom (`mom_rank` 0.0, `bench_mom_rank` 0.06). Quality extremes are unchanged (fundamentals-driven, so stale/fresh doesn't matter here): **MU** (0.922 watchlist, 0.927 within Technology, n=78) narrowly ahead of **NVDA** (0.921 / 0.926); **CMPS** still lowest in both populations (0.073 watchlist, 0.15 within Healthcare, n=54).

## What changed since 2026-09-21

- Half the board (24 of 42 symbols) produced no new candle and is still on 9/21's close — the dominant fact of this rebuild, not any single move.
- `weak` narrowed from six to four as **AAPL** and **AMD** both crossed their `volume_trend` above the 0.85 "light" threshold on fresh bars.
- `washout` membership held exactly at 9/21's five names; only **GLD** among them has fresh data.
- **XLF** posted the board's largest single-day `rvol` (2.262) on a real -1.968% decline, though its 20-day trend doesn't (yet) cross into `washout`.
- `reversal_tension` narrowed from six to five as **TSLA** cleared on fresh data.
- **MU**'s `mom_12_1` rose (+477.8% → +495.054%) on fresh data, reversing its recent easing.
- Cheap/rich valuation ends and `value_trap` are otherwise unchanged, mostly because the names involved didn't get a new print.

## Hawaii tier

All four Hawaii-lead names — **BOH, FHB, HE, MATX** — are among the stale half today: no new candle, so nothing here has genuinely changed since the 9/21 note, which already covered HE's `washout` streak and BOH's oversold RSI in detail. Worth flagging only because it means the tier that this dashboard exists partly to watch produced zero new information this session.

---
Tracker context, not trading advice. Nothing here is a recommendation to buy or sell any security.
