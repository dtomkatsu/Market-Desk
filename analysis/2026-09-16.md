# 2026-09-16

**XYL** (a held position) and **JETS** newly joined `washout` alongside **HE** and **RMIX** — the widest washout cohort of this note series — while **BOH** slipped below its 200-day average for the first time in this run.

## Volume

**IOT** held `confirmed` for a third straight session: day -2.377%, week +5.201%, month +4.256%, `rvol` 0.703 (below 1, the lowest reading yet on a confirmed day), `volume_trend` 1.24 (still expanding, up from 1.232 on 9/15), `up_down_ratio` 1.151, RSI14 53.67. The verdict first confirmed an +11.8% advance on 9/14; for a second straight session now it's holding on a decline instead of an advance — the character keeps diverging from what originally earned the label.

**XYL** (Xylem, a held position) newly reads `washout` — not flagged in either of the last two notes: day -0.214%, week -0.344%, month -8.336%, `rvol` 1.264, `volume_trend` 1.156 (expanding), `up_down_ratio` 0.726, RSI14 38.33, `range_52w_position` 0.04 — essentially a fresh 52-week low. Decline-on-heavy-volume, the pattern the heuristic is built to flag negatively.

**JETS** also newly reads `washout`: day -0.287%, week -0.785%, month -9.887%, `rvol` 1.687, `volume_trend` 1.179 (expanding), `up_down_ratio` 0.618, RSI14 35.16, `range_52w_position` 0.406. Same pattern — a multi-week decline now showing up on above-average volume.

**HE** held `washout` for a fifth straight session: day +0.196% (a rare up day within the streak), week -0.968%, month -13.378%, `rvol` 0.949, `volume_trend` 1.199 (still expanding but easing from 1.212 on 9/15), `up_down_ratio` 0.478, RSI14 22.13, `range_52w_position` 0.022 — still essentially a fresh 52-week low. Still the board's lone `value_trap` name. More in Hawaii tier below.

**RMIX** held `washout` for a second straight session: day +1.665% on the day, but week -7.507% and month -24.038% remain deeply negative, `rvol` 0.95, `volume_trend` 1.267 (still expanding), `up_down_ratio` 0.409 (still the board's most down-heavy), RSI14 26.86 (still oversold, up slightly from 24.18).

**GLD** cleared `washout` back to `quiet` after two sessions: day -0.611%, week -2.878%, month -3.391%, `rvol` 1.309, `volume_trend` 1.344 (still elevated despite the clearing verdict), `up_down_ratio` 1.504.

`weak` held at three names but membership fully turned over. **AAPL** is the lone holdover, a third straight session: day +0.323%, week +5.413%, month +8.776%, `rvol` 0.841, `volume_trend` 0.786 (contracting). **AMD** newly joined: day +1.646%, week -1.65%, month +1.285%, `rvol` 1.251, `volume_trend` 0.717 (contracting), `up_down_ratio` 1.011. **SPCX** also newly joined on its biggest up day this week: +5.15%, week +2.257%, month +3.18%, `rvol` 1.425, `volume_trend` 0.844 (contracting) — a sizable advance on a contracting participation trend. **MATX** and **MSFT** both cleared `weak` (to `mixed` and `quiet` respectively — MATX detail in Hawaii tier below).

Across the 42-symbol board: 1 `confirmed` (IOT), 4 `washout` (HE, JETS, RMIX, XYL), 3 `weak` (AAPL, AMD, SPCX), 15 `mixed`, 19 `quiet`. Versus 9/15: `washout` 3→4 (JETS and XYL joined, GLD cleared), `weak` unchanged in count at 3 but full turnover (AMD, SPCX in; MATX, MSFT out), `mixed` 16→15, `confirmed` and `quiet` unchanged at 1 and 19.

Breadth improved from 9/15's 13-up/29-down but stayed net negative: 16 up, 26 down, none flat — matching 9/14's exact split.

## Valuation

Cheap end (tracked universe) unchanged in order and essentially flat: **HE** (trailing P/E 7.822, `pe_percentile` 0.04), **FHB** (11.222, 0.08), **SU** (12.916, 0.12), **BOH** (13.862, 0.16) — all four moved less than half a point in trailing P/E from 9/15, none crossed a percentile. Within Technology, **NXT** remains the sector's cheapest name (trailing P/E 21.54, `pe_percentile` 0.091), unchanged.

Rich end unchanged in membership: **IOT** (1.0, Technology, trailing P/E 266.667, down from 271.067), **TSLA** (1.0, tracked universe, 342.243, up from 334.654) and **TT** (1.0, Industrials, 31.568, up slightly from 31.469) still tied at the top of their respective peer groups.

Widest trailing-to-forward P/E gaps, and for the first session in this series the order held with no swaps: **IOT** 266.667 → 42.127 (gap 224.5, narrowing for a fourth straight session, from 228.2). **TSLA** 342.243 → 166.639 (gap 175.6, widened from 168.8). **AMD** 139.767 → 35.007 (gap 104.8, widened from 97.5). **ALAB** 145.327 → 45.916 (gap 99.4, widened from 91.2). **MCHP** 104.147 → 15.513 (gap 88.6, widened slightly from 87.1). **DGII** 57.667 → 23.199 (gap 34.5, widened slightly from 33.3). **HLIT** 45.792 → 13.606 (gap 32.2, narrowed slightly from 32.7). **MU** 22.115 → 6.245 (gap 15.9, widened slightly from 15.0).

## Factors

`value_trap` is unchanged: **HE** remains the only name flagged, watchlist-relative (`value_score` 0.978, `quality_score` 0.248, bottom-third). Against the S&P 500 Utilities benchmark (n=31), `bench_value_trap` is still false — `bench_value_score` 0.983, `bench_quality_score` 0.4, unchanged from 9/15, mid-pack within-sector. A trap flag reads as caution, not a case for either side of a trade.

`reversal_tension` widened to thirteen names from twelve on 9/15: **MU** (`mom_12_1` +542.3% vs `ret_1m` -8.4%) and **TSLA** (`mom_12_1` -17.3% vs `ret_1m` +5.5%, re-flagged after clearing on 9/15) newly carry it, while **NVDA** cleared. **ALAB, AMZN, BOH, DGII, FHB, GHRS, HLIT, MCHP, META, NXT, TT** carry the flag unchanged.

Momentum extremes are unchanged in ranking: **MU** still tops both `mom_rank` (1.0) and `bench_mom_rank` (1.0, full S&P 500 cross-section), but `mom_12_1` jumped to +542.3% (from +519.0% on 9/15), reversing several sessions of easing. **META** remains at the bottom (`mom_rank` 0.0, `bench_mom_rank` 0.076, down from 0.094), `mom_12_1` -25.4% — more negative than 9/15's -21.7%, extending its slide for a second straight session. Quality extremes are also unchanged, with scores identical to 9/15 since these are fundamentals-driven and don't move intraday: **MU** (0.922 watchlist, 0.927 within Technology) narrowly ahead of **NVDA** (0.921 / 0.926); **CMPS** still lowest in both populations (0.073 watchlist, 0.15 vs the S&P 500).

## What changed since 2026-09-15

- Breadth improved to 16 up / 26 down from 13/29, but stayed net negative for a fourth straight session.
- **XYL** (a held position) and **JETS** newly joined `washout`; **GLD** cleared back to `quiet`.
- `weak` turned over completely: **AMD** and **SPCX** newly joined; **MATX** and **MSFT** cleared.
- **IOT** held `confirmed` for a third session, but for a second straight session the reading sits on a decline rather than the advance that first earned the label.
- **HE** extended `washout` to a fifth straight session.
- Cheap and rich valuation ends are unchanged; the trailing-to-forward P/E gap order held steady with no swaps for the first time this series.
- `reversal_tension` widened from twelve names to thirteen: **MU** and **TSLA** newly flagged; **NVDA** cleared.
- **MU**'s `mom_12_1` reversed its multi-session decline, jumping from +519.0% to +542.3%; **META**'s turned more negative again (-21.7% → -25.4%).
- **BOH** dropped below its 200-day SMA (was above it on 9/15) — see Hawaii tier.
- `value_trap` and quality extremes are otherwise unchanged from 9/15.

## Hawaii tier

**HE** is the story again — see Volume above: `washout` for a fifth straight session, day +0.196% (a rare up day within the streak), week -0.968%, month -13.378%, `up_down_ratio` 0.478, `volume_trend` still expanding at 1.199, RSI14 22.13, `range_52w_position` 0.022 (still essentially a fresh 52-week low), still the board's lone `value_trap` name, `reversal_tension` still false. **BOH** fell -0.756% (week -0.703%, month -5.369%), `rvol` 1.452, `divergence` quiet, RSI14 36.41, and dropped below its 200-day SMA today (`above_sma200` now false, was true on 9/15) — now below both its 50- and 200-day averages. Still carries `reversal_tension` (`mom_12_1` +23.3% vs `ret_1m` -5.4%). **FHB** fell -0.928% (week 0.0%, month -6.94%), `rvol` 1.571, `divergence` quiet, RSI14 37.43, still below both SMAs, still carrying `reversal_tension`. **MATX** fell -1.249% (week +3.861%, month +7.124%) on `rvol` 1.455, `divergence` cleared from `weak` to `mixed`, RSI14 67.38 — cooling from 9/15's 73.0 overbought reading — and `range_52w_position` 0.966, just off the fresh 52-week high. Still holding the strongest Hawaii-tier momentum rank (0.8 watchlist, 0.958 against the S&P 500).

---
Tracker context, not trading advice. Nothing here is a recommendation to buy or sell any security.
