# 2026-09-11

The board reversed hard from 9/10's 34-down washout into a 32-up, 10-down rally — but **HE** and **IOT** were the two names that didn't join it, both still reading `washout`.

## Volume

**HE** stayed in `washout` for a second straight session while the rest of the board rallied: day -0.86%, week -6.402%, month -12.992%, `rvol` 1.081 (above its 20-day average), `volume_trend` 1.181 (still expanding), `up_down_ratio` 0.477 (still down-heavy), RSI14 23.24 (lower than 9/10's 24.25), and `range_52w_position` 0.014 — essentially at its 52-week low. This is the decline-on-heavy-volume pattern the heuristic is built to catch. More in Hawaii tier below.

**IOT** also held `washout`: day -0.156%, week -0.955%, month -2.44%, `rvol` 0.946, `volume_trend` 1.18 (still expanding), `up_down_ratio` 1.207. A small down day, but the multi-week trend hasn't turned with the broader market's advance.

**GLD** cleared the `washout` it printed for the first time on 9/10, back to `quiet`: day +0.608%, `rvol` 0.908 (below average), `volume_trend` still elevated at 1.331, `up_down_ratio` 1.692.

The advance-on-contracting-volume pattern showed up widely today. **AAPL** (+1.745%, week +1.237%) and **AMD** (+2.488%, week +13.147%) both read `weak` — `rvol` 1.206 and 1.055 respectively, but `volume_trend` 0.781 and 0.712 (contracting), gains not backed by trend-level participation. **CMPS** (+0.914%, `rvol` 0.814, `volume_trend` 0.709) and **SPCX** (+2.045%, `rvol` 0.962, `volume_trend` 0.812) read the same way. **MATX** (+0.961%) read `weak` too — see Hawaii tier. **MU** was the board's one `weak` decliner: -0.22% on contracting `volume_trend` 0.64.

Several of the board's largest single-day gainers weren't flagged at all because `rvol` sat below or near 1: **MCHP** +3.66% (`rvol` 1.11, `volume_trend` 0.75, reads `quiet`), **HLIT** +3.093% (`rvol` 0.633, reads `quiet`), **ALAB** +2.351% (`rvol` 0.667, reads `quiet`). **RMIX** was the board's largest decliner at -4.298% (week -17.429%, month -30.089%) but on `rvol` only 0.713 — drift, not distribution, reading `mixed`. **IWM** printed the board's highest `rvol` at 1.501 on a modest +0.414% day, reading `mixed`.

Across the 42-symbol board: 0 `confirmed`, 2 `washout` (HE, IOT), 6 `weak` (AAPL, AMD, CMPS, MATX, MU, SPCX), 15 `mixed`, 19 `quiet`. Versus 9/10: `washout` 3→2 (GLD cleared), `weak` 4→6, `mixed` 14→15, `quiet` 21→19 — the extreme `washout` category thinned but didn't empty, since HE and IOT held.

## Valuation

Cheap end holds the same six names as 9/10 but **MU** and **NXT** swapped order within Technology: **MU**'s trailing P/E eased to 20.868 (from 22.118), moving it to `pe_percentile` 0.091 in Technology, while **NXT**'s ticked up to 21.349, moving it to 0.182 — the two traded places at the bottom of that peer group. The tracked-universe four are unchanged in order and near-identical in value: **HE** (trailing P/E 8.031, `pe_percentile` 0.04, tracked universe), **FHB** (11.209, 0.08, tracked universe), **SU** (12.679, 0.12, tracked universe), **BOH** (14.126, 0.16, tracked universe).

Rich end also unchanged in membership and order: **IOT** (1.0, Technology, trailing P/E 286.067, up from 256.267), **TSLA** (1.0, tracked universe, trailing P/E 326.336, down from 339.776) and **TT** (1.0, Industrials, trailing P/E 31.768, down from 32.799) still tied at the top of their respective peer groups.

Widest trailing-to-forward P/E gaps are the same cluster as 9/10, with **AMD** and **MCHP** now ahead of **ALAB**: **IOT** 286.067 → 45.485 (gap 240.6, widening further from 9/10's 215.5 and by far the largest on the board), **TSLA** 326.336 → 166.299 (gap 160.0, narrowing from 171.4), **AMD** 125.87 → 31.811 (gap 94.1), **MCHP** 105.118 → 15.658 (gap 89.5), **ALAB** 126.0 → 40.203 (gap 85.8), **HLIT** 46.875 → 13.928 (gap 33.0), **DGII** 52.897 → 21.28 (gap 31.6), **MU** 20.868 → 5.921 (gap 15.0).

## Factors

`value_trap` is unchanged: **HE** remains the only name flagged, watchlist-relative (`value_score` 0.978, `quality_score` 0.248, bottom-third). Against the S&P 500 Utilities benchmark (n=31), `bench_value_trap` is still false — `bench_value_score` 0.983, `bench_quality_score` 0.4 sits mid-pack within-sector.

`reversal_tension` narrowed to ten names from eleven on 9/10: **AMZN** cleared (`mom_12_1` +16.04% vs `ret_1m` -3.93%, no longer flagged). **ALAB, DGII, FHB, GHRS, LLY, MCHP, META, NXT, TSLA, TT** carry the flag unchanged.

Momentum extremes are unchanged in ranking: **MU** still tops both `mom_rank` (1.0) and `bench_mom_rank` (0.998, full S&P 500 cross-section), but `mom_12_1` ticked up to +551.97% — a small reversal after two straight sessions of easing (+610.8% on 9/4, +569.1% on 9/8, +543.2% on 9/10). **META** remains at the bottom (`mom_rank` 0.0, `bench_mom_rank` 0.092), `mom_12_1` -22.77%. Quality extremes also unchanged: **MU** (0.922 watchlist, 0.927 within Technology) narrowly ahead of **NVDA** (0.921 / 0.926); **CMPS** still lowest in both populations (0.073 watchlist, 0.15 vs the S&P 500).

## What changed since 2026-09-10

- The board reversed sharply: 32 up, 10 down, none flat — the sharpest one-day flip in this note series, versus 9/10's 8-up/34-down washout.
- **HE** and **IOT** did not join the rally — both held `washout` for a second straight session, HE on a continued decline to a fresh 52-week-range low (`range_52w_position` 0.014).
- **GLD**'s one-session `washout` (first ever in this series, printed 9/10) cleared back to `quiet`.
- `weak` grew from four names to six (**CMPS**, **SPCX** newly flagged) as the advance-on-contracting-volume pattern spread across the rally.
- **MU** and **NXT** swapped cheap-end order within the Technology peer group.
- **IOT**'s trailing-to-forward P/E gap widened further to 240.6, still the widest on the board; **AMD** and **MCHP** swapped order ahead of **ALAB** in that ranking.
- `reversal_tension` narrowed from eleven names to ten: **AMZN** cleared.
- **MU**'s `mom_12_1` ticked up to +551.97% after three straight sessions of easing — still by far the board's momentum leader.
- Quality extremes, the value-trap flag, and rich-end valuation are otherwise unchanged from 9/10.

## Hawaii tier

**HE** is the story again — see Volume above: still in `washout`, day -0.86%, week -6.402%, month -12.992%, `up_down_ratio` 0.477, `volume_trend` still expanding at 1.181, RSI14 23.24, `range_52w_position` 0.014 (essentially a 52-week low), still the board's lone `value_trap` name, `reversal_tension` still false. **FHB** rose +0.331% (week -0.942%, month -5.902%), `rvol` 1.134, `divergence` quiet, RSI14 38.87, still below both SMAs, still carrying `reversal_tension`. **BOH** was essentially flat at -0.026% (week -1.109%, month -3.698%), `rvol` 0.838, `divergence` quiet, RSI14 40.86, above its 200-day but not its 50-day average, no `reversal_tension`. **MATX** rose +0.961% (week +3.559%, month +7.335%) on `rvol` 1.061, `divergence` weak (advance on contracting volume, `volume_trend` 0.772), still holding the strongest Hawaii-tier momentum rank (0.8 watchlist, 0.954 against the S&P 500).

---
Tracker context, not trading advice. Nothing here is a recommendation to buy or sell any security.
