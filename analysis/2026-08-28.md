# 2026-08-28

GLD's `confirmed` divergence streak stretched to a fifth straight session, but today's session flipped from four days of advancing to a sharp -3.24% decline on the board's highest `rvol` (2.262) — a decline-on-heavy-volume print, not a continuation of the earlier up move.

## Volume

**GLD** posted the day's highest `rvol` at 2.262 (`volume_trend` 1.306, also expanding) on a -3.24% decline — its own worst single day in this note series, following four straight `confirmed` sessions on rising prices. `divergence` still reads `confirmed`, but today it is confirming a drop rather than an advance: week -3.42% (turning down after +1.77% last note), month still +8.41%, `up_down_ratio` 1.628, RSI14 54.56.

**HE** held `washout` for a fourth straight session: -0.52% on the day, week -0.87%, month now -13.57% (worse than the -12.39% in the last note), `rvol` 0.83 (below its own average even as `volume_trend` ticks up to 1.182), `up_down_ratio` 0.559, RSI14 29.54, still below both its 50- and 200-day averages.

**NVDA** gave back part of yesterday's outsized +8.74% move: -4.58% on the day, on above-average turnover (`rvol` 1.551, second-highest on the board), `up_down_ratio` 0.924, `divergence` `mixed` — a decline with real participation behind it, unlike several of today's other decliners.

**CMPS** was the largest single-name move on the board (-6.22%) but on volume close to its own average (`rvol` 0.98), `divergence` reads only `weak`. **NXT** (-4.62%) and **ALAB** (-4.81%) both fell on turnover below their own averages (`rvol` 0.481 and 0.62, the two lowest prints among today's decliners) — declines without matching participation.

Across the 42-symbol board: 1 `confirmed` (GLD), 1 `washout` (HE), 12 `weak`, 11 `mixed`, 17 `quiet` — a similar mix to recent sessions.

## Valuation

Cheap end is unchanged from the prior notes: **HE** (trailing P/E 8.837, `pe_percentile` 0.04, tracked universe), **FHB** (0.08), **MU** (0.091, within Technology), **SU** (0.12), **BOH** (0.16) — same five names, same order, same percentiles.

Rich end: **IOT** (1.0, Technology, trailing P/E 411.60) and **TT** (1.0, Industrials, trailing P/E 33.50) still tied at the top of their peer groups. **TSLA** is back on the board after yesterday's fetch failure, again just behind at 0.96 (tracked universe, trailing P/E 329.01).

Widest trailing-to-forward P/E gaps: **IOT** 411.60 → 45.41, **TSLA** 329.01 → 161.57, **ALAB** 141.90 → 45.28, **MCHP** 107.25 → 15.98, **AMD** 119.07 → 30.13 — the same cluster of names as prior notes, with TSLA back in its usual second spot.

## Factors

`value_trap` is unchanged: **HE** remains the only name flagged true, watchlist-relative (cheapest print, `quality_score` 0.248, bottom-third). Against the S&P 500 Utilities benchmark (n=31), `bench_value_trap` is still false — `bench_quality_score` 0.4 sits mid-pack within-sector.

`reversal_tension` now reads **FHB, HE, META, MSFT, NXT, TSLA** — six names. **AAPL** and **AMZN** cleared from yesterday's list; **META** is newly flagged (`mom_12_1` -27.64% fighting a `ret_1m` of +7.23%); **NXT** is re-flagged after clearing on 8/27 (`ret_1m` -11.83% against `mom_12_1` +43.15%); **TSLA** carries the flag again now that it's back on the board. **FHB, HE, MSFT** are unchanged.

Momentum extremes: **MU** still tops both `mom_rank` (1.0) and `bench_mom_rank` (0.998, full S&P 500), `mom_12_1` now +644%. At the bottom, it's **META** today rather than MSFT — `mom_rank` 0.0, `bench_mom_rank` 0.088 — since its 12-1 window is still deeply negative (-27.64%) despite the recent bounce; **MSFT** sits higher at 0.12 (`bench_mom_rank` 0.224). Within-sector quality has flipped: **MU**'s `bench_quality_score` (0.93) now edges out **NVDA**'s (0.926) in Technology, reversing the read in the last several notes where NVDA led; watchlist-relative `quality_score` is nearly tied (MU 0.922, NVDA 0.921). **CMPS** still holds the lowest `quality_score` in either population (0.073 watchlist, 0.154 against the S&P 500).

## What changed since 2026-08-27

- **GLD** extended `confirmed` to a fifth straight session, but the session itself broke from four days of advancing into a -3.24% decline on the board's highest `rvol` (2.262).
- **HE** held `washout` for a fourth straight session; month drawdown deepened to -13.57%.
- **NVDA** gave back part of yesterday's +8.74% surge, falling -4.58% on above-average volume.
- **TSLA** returned to the board after yesterday's fetch failure, back at `pe_percentile` 0.96 and carrying `reversal_tension` again.
- `reversal_tension`: **AAPL** and **AMZN** cleared; **META** newly flagged, **NXT** re-flagged.
- Within-sector quality flipped in Technology: **MU** now edges ahead of **NVDA** on `bench_quality_score`, having trailed it in recent notes.
- Cheap-end and rich-end valuation percentiles are otherwise unchanged.

## Hawaii tier

**HE** is again the story — see Volume above: fourth straight `washout` session, RSI14 29.54, still below both moving averages, `regime_verdict` confirmed, still the board's lone value-trap name. **FHB** eased -0.12% on the day (week -2.01%, month -6.75%), `rvol` 0.903, `divergence` quiet, RSI14 30.1, still below both SMAs, and still carries `reversal_tension`. **BOH** was essentially flat (+0.08% day), `rvol` 0.634, `divergence` quiet. **MATX** advanced +1.01% (week +0.68%, month +10.35%) on `rvol` 0.798, `divergence` mixed, still holding the strongest Hawaii-tier momentum rank (0.8 watchlist, 0.952 against the S&P 500).

---
Tracker context, not trading advice. Nothing here is a recommendation to buy or sell any security.
