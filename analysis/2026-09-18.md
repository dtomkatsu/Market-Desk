# 2026-09-18

Breadth flipped back to net negative (19 up, 23 down) after 9/17's record 35/7 split, `confirmed` emptied out for the first time in this note series as **IOT**'s four-session streak broke to `quiet`, and **HE** posted its worst single day of its `washout` run, RSI14 down to 17.62.

## Volume

No name holds `confirmed` today — **IOT** cleared after four straight sessions: day -1.125%, week +3.048%, month +0.893%, `rvol` 0.983 (essentially neutral, down from 9/17's already-low 0.467), `volume_trend` 1.254, `up_down_ratio` 1.004, RSI14 50.12. The verdict had been drifting in character for several sessions (confirming on declines rather than the +11.8% advance that first earned it on 9/14); today it simply stopped confirming anything.

`washout` held at five names, membership unchanged from 9/17: **GLD, HE, JETS, RMIX, XYL**.

**HE** extended `washout` to a seventh straight session, and it's the sharpest single day of the run: day -2.874%, week -5.588%, month -16.311% (accelerating from -12.716% on 9/17), `rvol` 2.895, `volume_trend` 1.281 (still expanding), `up_down_ratio` 0.394, RSI14 17.62 — a new low for the streak, down from 20.5 — `range_52w_position` 0.029, a fresh mark below 9/17's already-thin 0.04. Still the board's lone `value_trap` name; more in Hawaii tier below.

**RMIX** held `washout` for a fourth straight session: day -0.676%, week -0.973%, month -27.823% (deepening from -24.016%), `rvol` 0.765, `volume_trend` 1.277, `up_down_ratio` 0.38 — still the board's most down-heavy reading, narrowly ahead of HE's 0.394 — RSI14 25.93, still oversold, `range_52w_position` 0.247.

**XYL** (a held position) held `washout` for a third straight session, and today is the cleaner example of the pattern the label is built for: day -0.037%, week +0.561%, month -7.091%, `rvol` nearly doubled to 1.83 (from 0.915), `volume_trend` 1.209 (still expanding), `up_down_ratio` 0.709, RSI14 39.47, `range_52w_position` 0.046 — still essentially a fresh 52-week low.

**GLD** and **JETS** both held `washout` on up days again — the mismatch flagged in prior notes. **GLD**: day +0.705%, week +0.602%, month -3.062%, `rvol` 0.804, `volume_trend` 1.273, `up_down_ratio` 1.636. **JETS**: day +0.177%, week +0.177%, month -5.65%, `rvol` 1.209 (down sharply from 2.817), `volume_trend` 1.283, `up_down_ratio` 0.798.

`weak` widened to seven names from six, with **ALAB** the only addition: day +3.301%, week +4.131%, month +4.924%, `rvol` 2.077, `volume_trend` 0.824 (contracting) — an advance on a trend that hasn't broadened out. Holdovers: **AAPL** (day -0.258%, its first down day after a multi-session run of gains, `rvol` spiked to 2.098 — the board's heaviest single-day reading in this group — while `volume_trend` stayed at 0.811, still contracting), **AMD** (day +2.702%, `rvol` 1.744, `volume_trend` 0.768, still an advance-on-contracting-trend session), **CMPS** (day +1.267%, `rvol` 1.389, `volume_trend` 0.766), **MSFT** (day -0.798% — its own first down day within the streak, `rvol` jumped to 1.97 from 0.884, `volume_trend` 0.626, still the board's lowest trend reading — a decline-on-heavy-volume day sitting inside an otherwise contracting trend), **MU** (day +3.918%, `rvol` 1.467, `volume_trend` 0.654), **XLK** (day +0.819%, `rvol` 1.094, `volume_trend` 0.806).

Across the 42-symbol board: 0 `confirmed`, 5 `washout` (GLD, HE, JETS, RMIX, XYL), 7 `weak` (AAPL, ALAB, AMD, CMPS, MSFT, MU, XLK), 14 `mixed`, 16 `quiet`. Versus 9/17: `confirmed` 1→0 (IOT cleared), `weak` 6→7 (ALAB joined), `washout` and `mixed` unchanged in count, `quiet` 16→16 (IOT's move into `quiet` offset by another name leaving it).

Breadth reversed to net negative: 19 up, 23 down, none flat — versus 9/17's 35-up/7-down, the widest positive split in this note series to date.

## Valuation

Cheap end (tracked universe) unchanged in order: **HE** (trailing P/E 7.419, down from 7.597, `pe_percentile` 0.04), **FHB** (11.087, 0.08), **SU** (12.568, 0.12), **BOH** (13.535, 0.16) — all four multiples drifted down with no percentile crossings. Within Technology, **NXT** remains the sector's cheapest name (trailing P/E 21.486, up from 20.793, `pe_percentile` 0.091, unchanged) and **MU** holds just above it (23.571, `pe_percentile` 0.182, unchanged) — MU is also still the board's top momentum name (see Factors).

Rich end unchanged in membership: **IOT** (1.0, Technology, trailing P/E 253.667, down from 263.667), **TSLA** (1.0, tracked universe, 344.312, up from 343.651) and **TT** (1.0, Industrials, 32.265, up from 31.975) still tied at the top of their respective peer groups.

Widest trailing-to-forward P/E gaps, order held for a third straight session with no swaps: **IOT** 253.667 → 40.074 (gap 213.6, narrowing for a sixth straight session, from 222.0). **TSLA** 344.312 → 170.805 (gap 173.5, narrowed — the first narrowing after several straight widenings, from 177.9). **AMD** 157.02 → 39.531 (gap 117.5, widened from 106.9). **ALAB** 167.852 → 53.295 (gap 114.6, widened from 102.0). **MCHP** 109.971 → 16.381 (gap 93.6, widened slightly from 91.7). **DGII** 57.73 → 23.225 (gap 34.5, widened slightly from 34.2). **HLIT** 47.167 → 14.015 (gap 33.2, widened slightly from 32.5). **MU** 23.571 → 6.569 (gap 17.0, widened from 16.4).

## Factors

`value_trap` is unchanged: **HE** remains the only name flagged, watchlist-relative (`value_score` 0.978, `quality_score` 0.248, bottom-third). Against the S&P 500 Utilities benchmark (n=31), `bench_value_trap` is still false — `bench_value_score` 0.983, `bench_quality_score` 0.4, unchanged, mid-pack within-sector. A trap flag reads as caution, not a case for either side of a trade.

`reversal_tension` narrowed sharply again, to seven names from ten on 9/17: **COST** (`mom_12_1` -0.05% vs `ret_1m` -6.4%, both now negative so the signal and recent month agree), **FHB**, **MCHP** and **TSLA** cleared, while **GHRS** (`mom_12_1` +138.4% vs `ret_1m` -5.2%) newly (re-)carries it. **DGII, HLIT, LLY, META, NXT, TT** carry the flag unchanged.

Momentum extremes are unchanged in ranking: **MU** still tops both `mom_rank` (1.0) and `bench_mom_rank` (0.998, full S&P 500 cross-section), but `mom_12_1` eased again to +486.7% (from +493.3% on 9/17), continuing the pullback from 9/16's peak of +542.3%. **META** remains at the bottom (`mom_rank` 0.0, `bench_mom_rank` 0.07, up slightly from 0.062), `mom_12_1` -29.4% — a touch less negative than 9/17's -30.0% — while `ret_1m` eased to +21.9% from +25.5%, narrowing but not closing the 12-1-vs-recent-month divergence. Quality extremes are unchanged, scores identical to 9/17 since these are fundamentals-driven: **MU** (0.922 watchlist, 0.927 within Technology) narrowly ahead of **NVDA** (0.921 / 0.926); **CMPS** still lowest in both populations (0.073 watchlist, 0.15 vs the S&P 500).

## What changed since 2026-09-17

- Breadth flipped back to net negative: 19 up, 23 down, versus 9/17's 35-up/7-down record split.
- **IOT**'s four-session `confirmed` streak broke; the board now holds zero `confirmed` names.
- **ALAB** newly joined `weak` (seven names, up from six); no other membership changes in `weak` or `washout`.
- **HE** extended `washout` to a seventh straight session with its sharpest single-day decline of the run (-2.874%) and a new streak-low RSI14 of 17.62.
- Cheap and rich valuation ends unchanged in order; the trailing-to-forward P/E gap order held for a third straight session, though **TSLA**'s gap narrowed for the first time after several straight widenings while **AMD** and **ALAB** widened further.
- `reversal_tension` narrowed from ten names to seven: **COST, FHB, MCHP, TSLA** cleared; **GHRS** newly (re-)flagged.
- **MU**'s `mom_12_1` continued easing (+493.3% → +486.7%); **META**'s ticked less negative (-30.0% → -29.4%) as `ret_1m` cooled from +25.5% to +21.9%.
- `value_trap` and quality extremes are otherwise unchanged from 9/17.

## Hawaii tier

**HE** is the story again — see Volume above: `washout` for a seventh straight session, its worst single day of the run (-2.874%), week -5.588%, month -16.311% (deepening from -12.716%), `up_down_ratio` 0.394, `volume_trend` still expanding at 1.281, RSI14 17.62 (a new low for the streak), `range_52w_position` 0.029 (a fresh mark below 9/17's), still the board's lone `value_trap` name, `reversal_tension` still false. **BOH** was essentially flat (+0.08%, week -1.53%, month -3.197%), `rvol` jumped to 2.475 from 1.103, `divergence` quiet, RSI14 35.61 (little changed from 35.06), still below both its 50- and 200-day averages, `reversal_tension` still cleared. **FHB** fell -0.194% (week +0.019%, month -3.773%), `rvol` 2.284 (up sharply from 0.996), `divergence` mixed, RSI14 41.11 (little changed from 41.9), still below both SMAs, and its `reversal_tension` flag cleared today (see Factors). **MATX** fell -0.78% (week +1.19%, month +6.717%), `rvol` 2.526 (up from 1.007), `divergence` unchanged at mixed, RSI14 62.44 — cooling further from 9/17's 65.79 — `range_52w_position` 0.948, still near a 52-week high. Still holding the strongest Hawaii-tier momentum rank (0.8 watchlist, 0.964 against the S&P 500, unchanged).

---
Tracker context, not trading advice. Nothing here is a recommendation to buy or sell any security.
