# 2026-08-26

The day's most notable turnover print was **META**, whose `rvol` of 1.799 was the highest on the 42-symbol board — yet its own `divergence` reads `quiet` — while **GLD** extended its `confirmed` streak to a third straight session and **HE** held its `washout` read for a second.

## Volume

**META** posted the board's highest `rvol` (1.799) on a modest +1.07% day, week now +5.51%. `volume_trend` is 0.966 (roughly flat) and `up_down_ratio` 1.166. The elevated turnover didn't translate into a `confirmed` divergence — the verdict here is `quiet`, a reminder that the heuristic weighs more than a single day's relative volume.

**GLD** held `confirmed` for a third consecutive session, though today's price move was a decline: -1.58% on the day, `rvol` 0.88 (below average, down from 1.184 last note), but week +1.81% and month +14.06% with `up_down_ratio` 1.739 and RSI14 66.02 — the multi-session trend is still intact even on a down day.

**HE** stayed at `washout` for a second straight session: +1.06% on the day (a bounce), `rvol` 0.88 (below its own average), but week -1.88% and month -12.82%, `up_down_ratio` 0.597, RSI14 up slightly to 31.05 (from 26.63), still below both its 50- and 200-day averages.

**NVDA** gave the clearest decline-on-heavy-volume print: -1.59% on the day, `rvol` 1.261 (above average), week -3.63%, `up_down_ratio` 0.833 — a decline with participation behind it, `divergence` `mixed`.

**NXT** (+2.85%, the day's largest gain) and **ALAB** (+2.71%) both advanced on volume below their own averages — `rvol` 0.609 and 0.411 (the lowest on the board) respectively, `volume_trend` 0.855 and 0.856 (both contracting). Gains without matching turnover behind them.

**LLY** posted the day's largest decline (-3.59%, week -7.10%) on only slightly elevated volume (`rvol` 1.051), yet reads `quiet` on divergence — next earnings isn't until 2026-10-29, so nothing in this data explains the move; it's simply the day's biggest mover.

Overall divergence mix: 17 of 42 `mixed`, 13 `weak`, 10 `quiet`, GLD alone `confirmed`, HE alone `washout` — close to the prior session's distribution.

## Valuation

Cheap and rich ends of the board are unchanged from the last note. Cheapest: **HE** (trailing P/E 8.907, `pe_percentile` 0.04, tracked universe), **FHB** (0.08), **MU** (0.091, within Technology), **SU** (0.12), **BOH** (0.16) — same five names, same order, same percentiles. Richest: **IOT** (1.0, Technology, trailing P/E 398.4) and **TT** (1.0, Industrials, trailing P/E 33.90) tied at the top of their peer groups, **TSLA** (0.96, tracked universe, trailing P/E 323.20) close behind.

The widest trailing-to-forward P/E gaps: **IOT** 398.40 → 43.95, **TSLA** 323.20 → 160.21, **ALAB** 142.30 → 45.41, **MCHP** 107.32 → 16.22, **AMD** 122.06 → 31.07 — the same handful of names carrying most of the board's forward-multiple compression.

## Factors

`value_trap` is unchanged: **HE** remains the only name flagged true, watchlist-relative (cheapest print, `quality_score` 0.248, bottom-third). Against the S&P 500 Utilities benchmark (n=31), `bench_value_trap` is still false — `bench_quality_score` 0.4 sits mid-pack within-sector even though it reads bottom-third against this watchlist.

`reversal_tension` narrowed to six names — **AAPL, FHB, HE, MSFT, NXT, TSLA** — down from eight. **MCHP** and **TT** both cleared: their `ret_1m` figures eased to -1.6% and -1.4% against `mom_12_1` of +12.7% and +12.8%, no longer enough tension to flag.

Momentum and quality extremes are unchanged: **MU** still tops `mom_rank` (1.0, `mom_12_1` +606%) and `quality_score` (0.922) watchlist-side, and `bench_mom_rank` (0.998) against the full S&P 500; **MSFT** still sits at `mom_rank` 0.0 despite a +26.4% month, since the 12-1 window excludes the reversal-prone last month; **CMPS** still holds the lowest `quality_score` in either population (0.073 watchlist, 0.154 against the S&P 500). On within-sector quality, **NVDA** (`bench_quality_score` 0.942) still edges out MU (0.93) once measured against the full Technology sector.

## What changed since 2026-08-25

- **META** posted the day's highest `rvol` (1.799) in this note series — a new development, not seen in prior sessions — though its own divergence verdict stayed `quiet`.
- **GLD** held `confirmed` for a third straight session, this time on a down day (-1.58%) and lighter volume than the prior print.
- **HE** held `washout` for a second straight session, with a modest bounce (+1.06%) and improving (but still weak) RSI14.
- `reversal_tension` dropped **MCHP** and **TT**; no names newly flagged.
- Valuation percentiles at both ends of the distribution, and the `value_trap`/`bench_value_trap` split on HE, are unchanged.

## Hawaii tier

**HE** is again the story — see Volume above: second straight `washout` session, RSI14 improving slightly to 31.05 but still below both moving averages, `regime_verdict` confirmed, still the board's lone value-trap name. **FHB** was the tier's most active by turnover (`rvol` 1.098, `divergence` quiet) on a modest +0.70% day; month now -6.71%, RSI14 31.62, below both SMAs. **BOH** rebounded +1.43% on below-average volume (`rvol` 0.884, `quiet`) after the prior note's heavy-turnover down day. **MATX** eased -0.53% on the day (month +9.34%), still holding the strongest Hawaii-tier momentum rank (0.8 watchlist, 0.952 against the S&P 500).

---
Tracker context, not trading advice. Nothing here is a recommendation to buy or sell any security.
