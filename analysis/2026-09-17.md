# 2026-09-17

Breadth swung sharply positive to 35 up, 7 down — the best split anywhere in this note series, beating 9/11's 32/10 — but three of the day's biggest single-day gainers (AMD +6.36%, CMPS +8.70%, MU +5.50%) landed on a contracting `volume_trend`, pulling all three into `weak` alongside newly-returned MSFT and XLK.

## Volume

**IOT** held `confirmed` for a fourth straight session: day -1.623%, week +4.058%, month +1.061%, `rvol` 0.467 (the lowest reading yet on a confirmed day, below 9/16's 0.703), `volume_trend` 1.249 (still expanding, up slightly from 1.24), `up_down_ratio` 1.084, RSI14 51.56. This is the third straight session the verdict has sat on a decline rather than the +11.8% advance that first earned it on 9/14, now on record-low participation for the streak.

**GLD** rejoined `washout` after clearing to `quiet` on 9/16: day +1.69%, week +0.505%, month -0.048%, `rvol` 0.629, `volume_trend` 1.29 (elevated), `up_down_ratio` 1.61. Worth flagging: today's print is an up day, not the decline-on-heavy-volume pattern the label is built to catch.

**JETS** held `washout`: day +1.331%, week +1.185%, month -7.091%, `rvol` 2.817 (the board's largest reading), `volume_trend` 1.249 (expanding, up from 1.179), `up_down_ratio` 0.773, RSI14 40.03. Same caveat as GLD — today's move is positive, sitting on a heavy-volume spike rather than a decline.

**HE** held `washout` for a sixth straight session: day -1.369%, week -3.629%, month -12.716%, `rvol` 0.921, `volume_trend` 1.206 (still expanding), `up_down_ratio` 0.425, RSI14 20.5 (down from 22.13, a new low for the streak), `range_52w_position` 0.04 — still essentially a fresh 52-week low. Still the board's lone `value_trap` name; more in Hawaii tier below.

**RMIX** held `washout` for a third straight session: day -0.819%, week -4.585%, month -24.016%, `rvol` 0.724, `volume_trend` 1.251 (still expanding), `up_down_ratio` 0.408 — now the board's most down-heavy reading, edging past HE's 0.425 — RSI14 26.36 (little changed, still oversold), `range_52w_position` 0.253.

**XYL** (a held position) held `washout` for a second straight session: day +0.308%, week +0.985%, month -5.205%, `rvol` 0.915, `volume_trend` 1.166 (still expanding), `up_down_ratio` 0.786, RSI14 39.58, `range_52w_position` 0.047 — still near a fresh 52-week low.

`weak` widened to six names from three on 9/16, with only **AAPL** and **AMD** carrying over. **AAPL** held for what is now its fourth-plus straight session: day +1.381%, week +3.194%, month +8.699%, `rvol` 0.876, `volume_trend` 0.776 (contracting). **AMD** held a second session on a big up day: +6.359%, week +8.239%, month +12.531%, `rvol` 1.602, `volume_trend` 0.737 (contracting despite the day's size). **CMPS** newly joined on the board's largest single-day move: +8.696%, week +5.485%, month +7.914%, `rvol` 2.013, `volume_trend` 0.742 (contracting). **MSFT** re-entered after clearing on 9/16: +1.519%, week +1.078%, month +3.542%, `rvol` 0.884, `volume_trend` 0.596 — still the board's lowest trend reading. **MU** newly joined: +5.499%, week +0.009%, month +3.905%, `rvol` 0.9, `volume_trend` 0.631 (contracting). **XLK** newly joined: +2.245%, week +1.533%, month +1.315%, `rvol` 1.157, `volume_trend` 0.785. **SPCX** cleared to `mixed`.

Across the 42-symbol board: 1 `confirmed` (IOT), 5 `washout` (GLD, HE, JETS, RMIX, XYL), 6 `weak` (AAPL, AMD, CMPS, MSFT, MU, XLK), 14 `mixed`, 16 `quiet`. Versus 9/16: `washout` 4→5 (GLD rejoined), `weak` 3→6 (CMPS, MSFT, MU, XLK in; SPCX out), `mixed` 15→14, `quiet` 19→16.

Breadth reversed hard: 35 up, 7 down, none flat — versus 9/16's 16-up/26-down and four straight sessions of net-negative breadth. This is the widest positive split in the note series to date (previous best was 9/11's 32-up/10-down).

## Valuation

Cheap end (tracked universe) unchanged in order and essentially flat: **HE** (trailing P/E 7.597, `pe_percentile` 0.04), **FHB** (11.2, 0.08), **SU** (12.901, 0.12), **BOH** (13.874, 0.16) — no percentile crossings. Within Technology (n=81), **NXT** remains the sector's cheapest name (trailing P/E 20.793, `pe_percentile` 0.091, unchanged), and **MU** now sits just above it (22.94, `pe_percentile` 0.182, also Technology) — worth noting since MU is simultaneously the board's top momentum name (see Factors).

Rich end unchanged in membership: **IOT** (1.0, Technology, trailing P/E 263.667, down from 266.667), **TSLA** (1.0, tracked universe, 343.651, up from 342.243) and **TT** (1.0, Industrials, 31.975, up slightly from 31.568) still tied at the top of their respective peer groups.

Widest trailing-to-forward P/E gaps, order held for a second straight session with no swaps: **IOT** 263.667 → 41.654 (gap 222.0, narrowing for a fifth straight session, from 224.5). **TSLA** 343.651 → 165.761 (gap 177.9, widened from 175.6). **AMD** 142.811 → 35.953 (gap 106.9, widened from 104.8). **ALAB** 149.384 → 47.431 (gap 102.0, widened from 99.4). **MCHP** 107.75 → 16.05 (gap 91.7, widened from 88.6). **DGII** 57.246 → 23.03 (gap 34.2, narrowed slightly from 34.5). **HLIT** 46.25 → 13.743 (gap 32.5, widened slightly from 32.2). **MU** 22.94 → 6.497 (gap 16.4, widened from 15.9).

## Factors

`value_trap` is unchanged: **HE** remains the only name flagged, watchlist-relative (`value_score` 0.978, `quality_score` 0.248, bottom-third). Against the S&P 500 Utilities benchmark (n=31), `bench_value_trap` is still false — `bench_value_score` 0.983, `bench_quality_score` 0.4, unchanged, mid-pack within-sector. A trap flag reads as caution, not a case for either side of a trade.

`reversal_tension` narrowed sharply, to ten names from thirteen on 9/16: **COST** (`mom_12_1` +1.6% vs `ret_1m` -7.0%) and **LLY** (+61.3% vs -6.0%) newly carry it, while **ALAB, AMZN, BOH, GHRS** and **MU** cleared. **DGII, FHB, HLIT, MCHP, META, NXT, TSLA, TT** carry the flag unchanged.

Momentum extremes are unchanged in ranking but moved in magnitude: **MU** still tops both `mom_rank` (1.0) and `bench_mom_rank` (0.998, full S&P 500 cross-section), but `mom_12_1` eased sharply to +493.3% (from +542.3% on 9/16), reversing the prior session's jump. **META** remains at the bottom (`mom_rank` 0.0, `bench_mom_rank` 0.062, down from 0.076), `mom_12_1` -30.0% — more negative than 9/16's -25.4%, even as its `ret_1m` sits at +25.5%, the sharpest 12-1-vs-recent-month divergence on the board. Quality extremes are unchanged, scores identical to 9/16 since these are fundamentals-driven: **MU** (0.922 watchlist, 0.927 within Technology) narrowly ahead of **NVDA** (0.921 / 0.926); **CMPS** still lowest in both populations (0.073 watchlist, 0.15 vs the S&P 500) despite today's +8.7% single-day move.

## What changed since 2026-09-16

- Breadth swung sharply positive: 35 up, 7 down, versus 9/16's 16-up/26-down — the best split in this note series.
- **GLD** rejoined `washout` after clearing to `quiet` on 9/16.
- `weak` widened from three names to six: **CMPS**, **MSFT** (re-entering), **MU** and **XLK** newly joined; **SPCX** cleared to `mixed`.
- **IOT** held `confirmed` for a fourth session, again on a decline (-1.6%) and the lowest `rvol` yet (0.467) for this streak.
- **HE** extended `washout` to a sixth straight session, RSI14 down to 20.5, a new low for the run.
- Cheap and rich valuation ends are unchanged; the trailing-to-forward P/E gap order held for a second straight session with no swaps.
- `reversal_tension` narrowed from thirteen names to ten: **COST** and **LLY** newly flagged; **ALAB**, **AMZN**, **BOH**, **GHRS** and **MU** cleared.
- **MU**'s `mom_12_1` reversed its multi-session climb (+542.3% → +493.3%); **META**'s turned more negative again (-25.4% → -30.0%).
- `value_trap` and quality extremes are otherwise unchanged from 9/16.

## Hawaii tier

**HE** is the story again — see Volume above: `washout` for a sixth straight session, day -1.369%, week -3.629%, month -12.716%, `up_down_ratio` 0.425, `volume_trend` still expanding at 1.206, RSI14 20.5 (a new low for the streak), `range_52w_position` 0.04 (still essentially a fresh 52-week low), still the board's lone `value_trap` name, `reversal_tension` still false. **BOH** fell -0.374% (week -1.635%, month -4.828%), `rvol` 1.103, `divergence` quiet, RSI14 35.06 (down from 36.41), still below both its 50- and 200-day averages, and its `reversal_tension` flag cleared today (was true on 9/16 at `mom_12_1` +23.3% vs `ret_1m` -5.4%; now +23.7% vs -4.8%). **FHB** rose +0.781% (week +0.545%, month -5.18%), `rvol` 0.996, `divergence` quiet, RSI14 41.9 (up from 37.43), still below both SMAs, still carrying `reversal_tension`. **MATX** fell -0.369% (week +2.965%, month +7.545%) on `rvol` 1.007, `divergence` unchanged at `mixed`, RSI14 65.79 — cooling further from 9/16's 67.38 — and `range_52w_position` 0.96, still just off a fresh 52-week high. Still holding the strongest Hawaii-tier momentum rank (0.8 watchlist, 0.964 against the S&P 500, up from 0.958).

---
Tracker context, not trading advice. Nothing here is a recommendation to buy or sell any security.
