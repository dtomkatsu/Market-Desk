# 2026-09-08

The board's two long-running divergence streaks broke on the same day — GLD's `confirmed` run and HE's `washout` run both reverted to non-extreme reads — on the broadest down session since 9/1 (12 higher, 30 lower, none flat).

## Volume

**GLD** reads `quiet` today after multiple consecutive `confirmed` sessions in this note series (most recently reconfirmed on 9/4): day -1.73%, week -2.13%, `rvol` only 0.733 (below its 20-day average), `volume_trend` 1.328 (still expanding), `up_down_ratio` 1.634. The multi-week trend is still elevated, but today's below-average turnover on a down day isn't enough for the heuristic to call it a confirmed drop.

**HE** also broke its long `washout` run (held for at least six straight sessions through 9/1, still holding on 9/4): today reads `mixed` — day -1.71%, `rvol` 1.036, `volume_trend` 1.136 (still expanding), `up_down_ratio` 0.434 (still down-heavy), RSI14 25.69, still below both moving averages. More in Hawaii tier below.

**XYL** (held position) printed the board's highest `rvol` today at 2.122 on a +2.95% gain, but `divergence` reads `mixed`: `volume_trend` is 0.943, just under its expansion threshold, so the elevated single day doesn't confirm against the trend. It also sits near the bottom of its 52-week range (`range_52w_position` 0.072) despite today's gain.

**ALAB** posted the board's largest single-day move, -6.94% — a sharp reversal from 9/4's +9.75% — on `rvol` 1.682, but `divergence` reads `quiet`: `volume_trend` is 0.722 (contracting), so the decline isn't read as backed by trend-level participation.

**AMD** (+5.90%) and **CMPS** (+5.80%) were the board's two largest gainers, both on elevated `rvol` (1.598 and 1.546) but contracting `volume_trend` (0.692 and 0.666) — `divergence` reads `weak` on both, the advance-not-backed-by-trend pattern. **MU** (-1.61%) and **AAPL** (-1.17%) also read `weak`, in their case declines on contracting volume trend.

Across the 42-symbol board: 0 `confirmed`, 0 `washout`, 5 `weak` (AAPL, AMD, CMPS, MATX, MU), 13 `mixed`, 24 `quiet` — both extreme categories emptied out simultaneously; `weak` up from four on 9/4, `mixed` unchanged, `quiet` up from 23.

## Valuation

Cheap end is unchanged in both order and near-identical values from 9/4: **HE** (trailing P/E 8.45, `pe_percentile` 0.04, tracked universe), **FHB** (0.08, tracked universe), **NXT** (0.091, Technology), **SU** (0.12, tracked universe), **BOH** (0.16, tracked universe), **MU** (0.182, Technology).

Rich end also unchanged: **IOT** (1.0, Technology, trailing P/E 267.667), **TSLA** (1.0, tracked universe, trailing P/E 334.691) and **TT** (1.0, Industrials, trailing P/E 33.185) still tied at the top of their respective peer groups.

Widest trailing-to-forward P/E gaps are essentially the same cluster as 9/4, with modest drift: **IOT** 267.667 → 42.559 (gap 225.1), **TSLA** 334.691 → 170.557 (gap 164.1), **ALAB** 142.995 → 45.179 (gap 97.8), **AMD** 129.015 → 32.601 (gap 96.4), **MCHP** 107.912 → 16.074 (gap 91.8), **HLIT** 49.833 → 14.807 (gap 35.0), **DGII** 55.484 → 22.321 (gap 33.2), **MU** 22.62 → 6.452 (gap 16.2).

## Factors

`value_trap` is unchanged: **HE** remains the only name flagged, watchlist-relative (`value_score` 0.978, `quality_score` 0.248, bottom-third). Against the S&P 500 Utilities benchmark (n=31), `bench_value_trap` is still false — `bench_value_score` 0.983, `bench_quality_score` 0.4 sits mid-pack within-sector.

`reversal_tension` widened to twelve names from nine on 9/4: **ALAB, AMZN, DGII, GHRS, GOOG, HE, NXT, TSLA, TT** carry the flag unchanged; **FHB** (`mom_12_1` +10.17% vs `ret_1m` -5.22%) and **MCHP** (+31.05% vs -12.83%) — both cleared on 9/4 — are flagged again, and **LLY** (+63.84% vs -5.08%) is newly flagged for the first time in this note series.

Momentum extremes are unchanged: **MU** still tops both `mom_rank` (1.0) and `bench_mom_rank` (0.998, full S&P 500 cross-section), `mom_12_1` +569.1% (down from +610.8% on 9/4). **META** remains at the bottom (`mom_rank` 0.0, `bench_mom_rank` 0.118), `mom_12_1` -21.05%. Quality extremes also unchanged: **MU** (0.922 watchlist, 0.927 within Technology) narrowly ahead of **NVDA** (0.921 / 0.926); **CMPS** still lowest in both populations (0.073 watchlist, 0.154 vs the S&P 500).

## What changed since 2026-09-04

- The board turned broadly lower: 12 up, 30 down, none flat — the broadest down session since 9/1's 34-down sell-off, reversing 9/4's roughly even split.
- **GLD**'s multi-session `confirmed` streak and **HE**'s multi-session `washout` streak both broke on the same day, moving to `quiet` and `mixed` respectively — the board's two long-running divergence extremes emptied out together, leaving zero `confirmed` and zero `washout` names.
- **ALAB** swung from 9/4's largest single-day gain (+9.75%) to today's largest single-day decline (-6.94%), both times read `quiet`/`quiet` on contracting `volume_trend`.
- `reversal_tension` widened from nine names to twelve: **FHB** and **MCHP** are flagged again after clearing on 9/4; **LLY** is newly flagged.
- Divergence mix shifted: `weak` 4→5, `confirmed` 1→0, `washout` 1→0, `quiet` 23→24, `mixed` unchanged at 13.
- Cheap-end and rich-end valuation, and momentum/quality extremes, otherwise unchanged from 9/4.

## Hawaii tier

**HE** is the story again — see Volume above: its `washout` divergence streak broke, moving to `mixed` (day -1.71%, `up_down_ratio` 0.434, `volume_trend` still expanding at 1.136), RSI14 25.69, still below both moving averages, still the board's lone `value_trap` name, and still carrying `reversal_tension`. **FHB** fell -1.00% (week +0.78%, month -5.22%), `rvol` 1.068, `divergence` quiet, RSI14 38.18, still below both SMAs, and is newly back in `reversal_tension`. **BOH** fell -1.30% (week +0.34%, month -3.02%), `rvol` 0.815, `divergence` quiet, RSI14 41.62, above its 200-day but not its 50-day average, no `reversal_tension`. **MATX** fell -1.02% (week -0.68%, month +7.99%) on `rvol` 0.788, `divergence` weak, still holding the strongest Hawaii-tier momentum rank (0.8 watchlist, 0.944 against the S&P 500).

---
Tracker context, not trading advice. Nothing here is a recommendation to buy or sell any security.
