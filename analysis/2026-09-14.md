# 2026-09-14

**IOT** printed the board's first `confirmed` divergence verdict of this note series — an +11.8% day backed by expanding trend volume — while **HE** extended its `washout` streak to a third straight session and **GLD** flipped back into `washout` after clearing it on 9/11.

## Volume

**IOT**: +11.803% (week +6.741%, month +4.302%), `rvol` 1.389, `volume_trend` 1.212 (expanding), `up_down_ratio` 1.236, `divergence` **confirmed** — the first `confirmed` reading in this note series (9/10 and 9/11 both closed at zero). An advance this size backed by trend-level participation is the pattern the heuristic is built to flag positively.

**HE** held `washout` for a third straight session: day -0.193%, week -6.583%, month -12.426%, `rvol` 0.998, `volume_trend` 1.195 (still expanding), `up_down_ratio` 0.441 (still down-heavy), RSI14 23.01, `range_52w_position` 0.026 — essentially a fresh 52-week low. Still the decline-on-heavy-volume pattern. More in Hawaii tier below.

**GLD** returned to `washout` after clearing to `quiet` on 9/11: day -1.487%, week -3.425%, month -1.534%, `rvol` 0.611 (below its 20-day average), `volume_trend` 1.349 (still elevated), `up_down_ratio` 1.58.

The advance-on-contracting-volume pattern (`weak`) held five names, two fewer than 9/11's six: **AAPL** (+0.244%, week +4.097%, month +9.114%), `rvol` 0.923, `volume_trend` 0.792 (contracting), `up_down_ratio` 1.231. **CMPS** (+1.324%, week +9.57%), `rvol` 1.033, `volume_trend` 0.725. **MATX** (+2.798%, week +4.889%) — see Hawaii tier. **SPCX** (-2.024% on the day but `rvol` 0.849, `volume_trend` 0.816, still reading weak on its longer trend). **MSFT** newly joined: +1.973% (week +1.143%, month +1.908%), `rvol` 1.11, `volume_trend` 0.6 — the lowest `volume_trend` on the board today, `up_down_ratio` 1.882 (the board's highest), a strong price move on a strongly contracting participation trend.

**AMD** and **MU** both cleared `weak` back to `quiet` despite sizable down days: **AMD** -4.402% (`rvol` 1.377, `volume_trend` 0.712) and **MU** -5.253% (`rvol` 1.04, `volume_trend` 0.644) — declines on contracting volume read as drift rather than distribution under this heuristic, not flagged either way.

Across the 42-symbol board: 1 `confirmed` (IOT), 2 `washout` (GLD, HE), 5 `weak` (AAPL, CMPS, MATX, MSFT, SPCX), 15 `mixed`, 19 `quiet`. Versus 9/11: `confirmed` 0→1, `washout` 2→2 (membership changed: IOT out, GLD back in), `weak` 6→5, `mixed` and `quiet` unchanged at 15 and 19.

Breadth flipped back toward weakness: 16 up, 26 down, none flat — a reversal from 9/11's 32-up/10-down rally, though nowhere near 9/10's 8-up/34-down washout session.

## Valuation

Cheap end: the tracked-universe four are unchanged in order and near-flat — **HE** (trailing P/E 7.915, `pe_percentile` 0.04, tracked universe), **FHB** (11.239, 0.08, tracked universe), **SU** (13.309, 0.12, tracked universe), **BOH** (14.02, 0.16, tracked universe). Within Technology, **NXT** and **MU** swapped back to 9/10's order: **NXT**'s trailing P/E eased to 20.618 (from 21.349 on 9/11), moving it to `pe_percentile` 0.091, while **MU**'s ticked up to 20.958 (from 20.868), moving it to 0.182 — the reverse of the swap noted on 9/11.

Rich end unchanged in membership: **IOT** (1.0, Technology, trailing P/E 277.667, down from 286.067), **TSLA** (1.0, tracked universe, trailing P/E 330.167, up from 326.336) and **TT** (1.0, Industrials, trailing P/E 31.5, down from 31.768) still tied at the top of their respective peer groups.

Widest trailing-to-forward P/E gaps: **IOT** 277.667 → 44.149 (gap 233.5) — still by far the largest on the board, but the first session in this series where it narrowed rather than widened (from 240.6 on 9/11). **TSLA** 330.167 → 165.192 (gap 165.0, widened from 160.0). **AMD** 129.282 → 32.381 (gap 96.9, widened from 94.1). **MCHP** 105.132 → 15.66 (gap 89.5, flat). **ALAB** 124.404 → 39.5 (gap 84.9, narrowed slightly from 85.8). **HLIT** 48.417 → 14.386 (gap 34.0). **DGII** 54.44 → 21.901 (gap 32.5). **MU** 20.958 → 5.944 (gap 15.0, flat).

## Factors

`value_trap` is unchanged: **HE** remains the only name flagged, watchlist-relative (`value_score` 0.978, `quality_score` 0.248, bottom-third). Against the S&P 500 Utilities benchmark (n=31), `bench_value_trap` is still false — `bench_value_score` 0.983, `bench_quality_score` 0.4 sits mid-pack within-sector.

`reversal_tension` widened to eleven names from ten on 9/11: **GHRS** cleared (`mom_12_1` +125.9% vs `ret_1m` -1.1%, no longer flagged), while **HLIT** (`mom_12_1` +29.2% vs `ret_1m` -14.0%) and **NVDA** (`mom_12_1` +27.3% vs `ret_1m` -6.3%) are newly flagged. **ALAB, DGII, FHB, LLY, MCHP, META, NXT, TSLA, TT** carry the flag unchanged.

Momentum extremes are unchanged in ranking: **MU** still tops both `mom_rank` (1.0) and `bench_mom_rank` (0.998, full S&P 500 cross-section), with `mom_12_1` easing further to +531.8% (from +551.97% on 9/11, continuing a multi-session decline). **META** remains at the bottom (`mom_rank` 0.0, `bench_mom_rank` 0.106, up from 0.092), `mom_12_1` -20.5% (less negative than 9/11's -22.77%). Quality extremes also unchanged: **MU** (0.922 watchlist, 0.927 within Technology) narrowly ahead of **NVDA** (0.921 / 0.926); **CMPS** still lowest in both populations (0.073 watchlist, 0.15 vs the S&P 500).

## What changed since 2026-09-11

- Breadth flipped back toward weakness: 16 up, 26 down, none flat — versus 9/11's 32-up/10-down rally.
- **IOT** printed the board's first `confirmed` divergence verdict of this note series, moving out of `washout` on an +11.8% day backed by expanding volume trend.
- **GLD** flipped back into `washout` after clearing to `quiet` on 9/11.
- **HE** held `washout` for a third straight session, extending its decline to a fresh `range_52w_position` of 0.026.
- `weak` eased from six names to five: **AMD** and **MU** cleared (both read `quiet` despite 4-5% down days on contracting volume); **MSFT** newly flagged.
- **NXT** and **MU** swapped cheap-end order within Technology again, reverting to 9/10's ordering.
- **IOT**'s trailing-to-forward P/E gap narrowed for the first time this series (240.6 → 233.5), while still the board's widest; **TSLA**'s and **AMD**'s gaps both widened.
- `reversal_tension` widened from ten names to eleven: **GHRS** cleared; **HLIT** and **NVDA** newly flagged.
- **MU**'s `mom_12_1` continued easing (+551.97% → +531.8%); **META**'s turned less negative (-22.77% → -20.5%).
- `value_trap` and quality extremes are otherwise unchanged from 9/11.

## Hawaii tier

**HE** is the story again — see Volume above: `washout` for a third straight session, day -0.193%, week -6.583%, month -12.426%, `up_down_ratio` 0.441, `volume_trend` still expanding at 1.195, RSI14 23.01, `range_52w_position` 0.026 (essentially a fresh 52-week low), still the board's lone `value_trap` name, `reversal_tension` still false. **FHB** rose +0.097% (week -1.112%, month -6.588%), `rvol` 0.867, `divergence` quiet, RSI14 39.4, still below both SMAs, still carrying `reversal_tension`. **BOH** rose +0.264% (week -1.542%, month -3.964%), `rvol` 1.062, `divergence` quiet, RSI14 42.38, above its 200-day but not its 50-day average, no `reversal_tension`. **MATX** rose +2.798% (week +4.889%, month +10.694%) on `rvol` 1.599, `divergence` weak (advance on contracting volume, `volume_trend` 0.799), RSI14 71.53 — now in overbought territory — and `range_52w_position` 0.972, near a fresh 52-week high. Still holding the strongest Hawaii-tier momentum rank (0.8 watchlist, 0.952 against the S&P 500).

---
Tracker context, not trading advice. Nothing here is a recommendation to buy or sell any security.
