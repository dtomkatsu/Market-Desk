# 2026-09-23

The board's broadest one-day decline of this run — 30 of 42 tracked names closed lower against just 12 higher, all on fresh bars — was led by **Bank of Hawaii**, whose 2.475x relative volume is the highest single reading on today's board and whose RSI14 fell to a fresh oversold low of 21.13.

## Volume

No name holds `confirmed` today — zero for a fourth straight session (9/18, 9/21, 9/22, 9/23).

`washout` sits at five names, membership turned over from 9/22: **GLD** cleared to `mixed` (day -1.797%, `up_down_ratio` 1.625, still an up_down_ratio-above-1-vs-washout-label mismatch when it held the flag, now resolved) and **JETS** (re-)entered. Continuing members: **HE, IOT, RMIX, XYL**.

**HE**: day -3.657%, week -9.696%, month -19.826% (deepening again), `rvol` 0.89 (below average), `volume_trend` 1.295 (still expanding), `up_down_ratio` 0.362, RSI14 13.37 — a fresh low for this run, `regime_verdict` **confirmed**. The board's clearest decline-on-expanding-participation read.

**JETS**: day -2.62%, week +1.327%, month -4.464%, `rvol` 1.909 (second-highest on the board today), `volume_trend` 1.365 (the board's most expanding), `up_down_ratio` 0.847 — a real heavy-volume decline, a clean `washout` read.

**RMIX**: day -5.564%, week -3.634%, month -27.423%, `rvol` 0.609 (light), `volume_trend` 1.3 (expanding), `up_down_ratio` 0.381 (the board's most down-heavy reading), RSI14 25.54.

**IOT** and **XYL** both carry `washout` on up days — the same divergence-heuristic mismatch flagged in prior notes, now showing on two names at once: IOT day +2.707% (`rvol` 0.474, the board's lightest, `volume_trend` 1.283, `up_down_ratio` 0.978, RSI14 48.99); XYL day +1.305% (`rvol` 0.728, `volume_trend` 1.245, `up_down_ratio` 0.707, RSI14 44.4). Both are being flagged by the 20-day expanding-trend component, not by today's direction.

`weak` narrowed to two names from four: **MSFT** and **CMPS** both cleared to `quiet`. Remaining: **MU** (day -2.215%, week +15.685%, month +17.733%, `rvol` 0.859, `volume_trend` 0.711 — the board's lowest trend reading, still an advance-heavy month riding a contracting 20-day trend) and **XLK** (day +0.251%, week +6.437%, month +6.687%, `rvol` 0.879, `volume_trend` 0.843 — the sector-ETF echo of the same pattern).

Across the 42-symbol board: 0 `confirmed`, 5 `washout`, 2 `weak`, 23 `mixed`, 12 `quiet`. Breadth: 12 up, 30 down, none flat — the most lopsided decline of the recent stretch (versus 9/21's 31 up/11 down and 9/22's roughly flat fresh-only read). Heaviest single-day `rvol` reading on the board: **BOH** at 2.475, well above the next-highest **JETS** (1.909) and **TLT** (1.684). Lightest: **IOT** (0.474) and **HLIT** (0.516).

## Valuation

Cheap end (tracked universe, n=11) held its exact order and percentiles from 9/22 despite every name repricing today — the moves were broadly correlated enough that relative standing didn't shift: **HE** (trailing P/E 7.164, `pe_percentile` 0.04), **FHB** (10.781, 0.08), **SU** (12.856, 0.12), **BOH** (13.075, 0.16). Within Technology (n=11), **NXT** remains cheapest (20.664, `pe_percentile` 0.091) with **MU** just above it (24.408, 0.182), also unchanged from 9/22.

Rich end unchanged in membership and order, all at the top of their groups: **IOT** (1.0, Technology, trailing P/E 262.133, up from 9/22's 260.533), **TSLA** (1.0, tracked universe, 356.547, up from 348.734) and **TT** (1.0, Industrials, 32.678, up from 32.522).

Widest trailing-to-forward P/E gaps, order unchanged from 9/22: **IOT** 262.133 → 41.411 (gap 220.7). **TSLA** 356.547 → 172.007 (gap 184.5, widened sharply from 175.7). **ALAB** 178.47 → 56.387 (gap 122.1). **AMD** 160.117 → 40.413 (gap 119.7). **MCHP** 109.838 → 16.295 (gap 93.5). **DGII** 60.913 → 24.505 (gap 36.4). **HLIT** 45.604 → 13.551 (gap 32.1, essentially flat). **MU** 24.408 → 6.791 (gap 17.6).

## Factors

`value_trap` unchanged: **HE** remains the only name flagged, watchlist-relative (`value_score` 0.978, `quality_score` 0.248, bottom-third). Against the S&P 500 Utilities benchmark (n=31), `bench_value_trap` is still false (`bench_value_score` 0.983, `bench_quality_score` 0.4). A trap flag is caution, not a case for either side.

`reversal_tension` widened to eight names from five: **FHB** (`mom_12_1` +6.731% vs `ret_1m` -5.851%) and **GHRS** (+112.528% vs -8.366%) are newly flagged; **TSLA** is re-flagged after clearing on 9/22, and with a wider gap than when it last carried the flag on 9/21 (`mom_12_1` -19.636% vs `ret_1m` +8.933%, versus 9/21's -17.205%/+8.742%). Continuing: **BOH** (+19.746% vs -8.409%), **HLIT** (+22.135% vs -11.868%), **LLY** (+67.997% vs -8.317%), **META** (-26.751% vs +33.213%), **NXT** (+20.785% vs -5.071%). A trap flag reads as caution; this flag reads as "last month fought the 12-1 signal," not a signal itself.

Momentum: **MU** still tops both `mom_rank` (1.0) and `bench_mom_rank` (0.996, full S&P 500 cross-section), but its `mom_12_1` fell sharply to +453.937% from 9/22's +495.054% — the pullback that had briefly reversed on 9/22 has resumed. **META** remains at the bottom (`mom_rank` 0.0, `bench_mom_rank` 0.068), `mom_12_1` -26.751%, a touch less negative than 9/22's -29.122%. Quality extremes are unchanged (fundamentals-driven): **MU** (0.922 watchlist, 0.927 within Technology, n=81) narrowly ahead of **NVDA** (0.921 / 0.926); **CMPS** still lowest in both populations (0.073 watchlist, 0.15 within the S&P 500, n=503).

## What changed since 2026-09-22

- Every symbol shows a genuine 9/23 bar — 9/22's half-stale board is fully fresh today.
- Breadth flipped sharply negative: 12 up / 30 down, the most lopsided decline session of this run.
- `washout` turned over: **GLD** cleared to `mixed`; **JETS** (re-)entered. **HE, IOT, RMIX, XYL** held.
- `weak` narrowed from four to two as **MSFT** and **CMPS** both cleared to `quiet`.
- `confirmed` stayed at zero for a fourth straight session.
- **MU**'s `mom_12_1` reversed lower again (+495.054% → +453.937%) on fresh data, resuming its recent pullback.
- `reversal_tension` widened from five to eight names: **FHB** and **GHRS** newly flagged, **TSLA** re-flagged.
- Cheap/rich valuation ends and the P/E-gap ranking held their exact 9/22 order even with fresh prices across the board.
- `value_trap` and quality extremes are otherwise unchanged.

## Hawaii tier

All four Hawaii-lead names — **BOH, FHB, HE, MATX** — declined together on fresh data today, after half the tier was stale on 9/22. **BOH** led the board's volume readings (`rvol` 2.475, the day's highest) on a -3.406% day (week -6.748%, month -8.409%), RSI14 falling to a fresh streak-low of 21.13, still below both its 50- and 200-day averages, and still carrying `reversal_tension` (`mom_12_1` +19.746% vs `ret_1m` -8.409%). **FHB** fell -2.824% (week -4.139%, month -5.851%), `rvol` 1.442, RSI14 28.92, also carrying `reversal_tension`. **HE** extended its `washout` streak — see Volume above — with `regime_verdict` still **confirmed**, still the board's lone `value_trap`. **MATX** posted the tier's steepest single-day move at -5.458% (week -6.995%, month -0.399%) on `rvol` 0.795 and a `quiet` divergence read — the decline was not accompanied by unusual participation — while still holding the tier's strongest momentum rank (`mom_rank` 0.88, `bench_mom_rank` 0.97 against the full S&P 500).

---
Tracker context, not trading advice. Nothing here is a recommendation to buy or sell any security.
