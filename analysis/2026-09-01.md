# 2026-09-01

A broad, mostly quiet sell-off hit 34 of the 42 tracked names, while GLD's `confirmed` divergence streak extended to a seventh straight session — flipping back to confirming a decline, as it did on 8/28 — and RMIX printed the highest single-day `rvol` (3.781) seen anywhere in this note series without the heuristic calling it significant.

## Volume

**GLD** extended `confirmed` to a seventh consecutive session: day -2.86%, week -7.32%, month +6.74%, `rvol` 1.056, `volume_trend` 1.356 (still expanding), `up_down_ratio` 1.559, RSI14 46.0. That reverses 8/31's read — this is a decline on expanding volume, the same "confirming a drop" pattern seen on 8/28, not the advancing move flagged last note.

**HE** held `washout` for a sixth straight session: day -1.25%, week -2.55%, month -12.76% (marginally less negative than -13.16% on 8/31), `rvol` 1.057, `volume_trend` 1.189 (still expanding), `up_down_ratio` 0.558, RSI14 24.67 — more oversold than last note's 26.67 — still below both its 50- and 200-day averages.

Two of today's patterns are worth naming explicitly. **AAPL** rose +2.61% (the board's second-largest gain) on a `volume_trend` of 0.738 — contracting — `divergence` reads only `weak`: an advance on contracting volume, thinly supported. By contrast, **HE** above is the clean decline-on-heavy-volume case: a falling price with volume_trend still expanding, which is exactly what keeps its `washout` verdict alive.

**RMIX** posted the board's highest `rvol` at 3.781 — the highest single-day print anywhere in this note series — falling -5.54% on it, yet `divergence` still reads `quiet` because its `volume_trend` (0.816) sits below average: one elevated day inside an otherwise below-average multi-week trend, the same disconnect noted for RMIX and JETS on 8/31. **JETS** (`rvol` 1.92, day -2.14%) reads `mixed` rather than quiet today, since its own `volume_trend` (0.875) is closer to its threshold.

Other large single-day moves without a strong volume read: **ALAB** -5.75% (`rvol` 0.847, `divergence` quiet — below-average turnover), **DGII** -5.08% (`rvol` 1.139, quiet), **IOT** -4.38% (`rvol` 0.98, quiet). **SU** was the day's best mover among individual names at +3.25% (`rvol` 0.689, `divergence` mixed).

Across the 42-symbol board: 1 `confirmed` (GLD), 1 `washout` (HE), 6 `weak`, 10 `mixed`, 24 `quiet` — quieter on `weak` (down from 9) but a touch more `mixed` (up from 8) and `quiet` (up from 23) than 8/31.

## Valuation

Cheap end flip-flopped back to its pre-8/31 order: **HE** (trailing P/E 8.589, `pe_percentile` 0.04, tracked universe), **FHB** (0.08, tracked universe), **MU** (0.091, Technology), **SU** (0.12, tracked universe), **BOH** (0.16, tracked universe). **MU** returns to the third spot as its price pulled back today (-2.64%); **NXT** moves back out to 0.182 (Technology) — the exact reverse of the swap noted on 8/31.

Rich end is unchanged: **IOT** (1.0, Technology, trailing P/E 390.6) and **TT** (1.0, Industrials, trailing P/E 32.80) still tied at the top of their peer groups, with **TSLA** close behind at 0.96 (tracked universe, trailing P/E 332.79).

Widest trailing-to-forward P/E gaps: **IOT** 390.6 → 43.09, **TSLA** 332.79 → 164.97, **ALAB** 138.57 → 43.78, **MCHP** 105.02 → 15.64, **AMD** 117.55 → 29.75 — the same cluster as prior notes, all drifting down slightly with today's broader decline.

## Factors

`value_trap` is unchanged: **HE** remains the only name flagged true, watchlist-relative (cheapest print, `quality_score` 0.248, bottom-third). Against the S&P 500 Utilities benchmark (n=31), `bench_value_trap` is still false — `bench_quality_score` 0.4 sits mid-pack within-sector.

`reversal_tension` widened to eight names — **ALAB, AMD, AMZN, BOH, FHB, GOOG, NXT, TSLA** — up from six on 8/31. **HE** and **MSFT** cleared; **ALAB** (`mom_12_1` +76.2% against `ret_1m` -12.8%), **AMD** (+198.0% vs -5.2%), **AMZN** (+24.0% vs -10.2%) and **BOH** (+21.1% vs -5.3%) are newly flagged — in each case a positive 12-1 formation return meeting a negative reversal month, a pattern the board's broad sell-off pushed into more names today. **FHB, GOOG, NXT, TSLA** carry the flag unchanged.

Momentum extremes are unchanged from 8/31: **MU** still tops both `mom_rank` (1.0) and `bench_mom_rank` (0.998, full S&P 500), `mom_12_1` now +598%. **META** sits at the bottom (`mom_rank` 0.0, `bench_mom_rank` 0.128) on a `mom_12_1` of -19.8%. Within Technology, `bench_quality_score` still has **MU** (0.927) narrowly ahead of **NVDA** (0.926), with watchlist-relative scores nearly tied (0.922 vs 0.921) — the gap has narrowed further but the order hasn't changed. **CMPS** still holds the lowest `quality_score` in either population (0.073 watchlist, 0.154 against the S&P 500).

## What changed since 2026-08-31

- A broad decline hit 34 of 42 names, led by **ALAB** (-5.75%), **RMIX** (-5.54%), **DGII** (-5.08%) and **IOT** (-4.38%).
- **GLD** extended `confirmed` to a seventh straight session, flipping back to confirming a decline (-2.86% on expanding volume) after last note's advancing read.
- **HE** held `washout` for a sixth straight session; month drawdown eased slightly to -12.76% from -13.16%.
- **RMIX** printed the highest single-day `rvol` (3.781) seen in this note series, still read as `quiet` on a below-average multi-week trend.
- Cheap-end Technology reversed itself: **MU** back to `pe_percentile` 0.091, **NXT** back out to 0.182 — the exact swap from 8/31, undone.
- `reversal_tension` widened from six to eight names: **HE, MSFT** cleared; **ALAB, AMD, AMZN, BOH** newly flagged.
- Divergence mix shifted: `weak` 9→6, `mixed` 8→10, `quiet` 23→24 (`confirmed`/`washout` unchanged at one each).
- Rich-end valuation and momentum/quality extremes otherwise unchanged.

## Hawaii tier

**HE** is again the story — see Volume above: sixth straight `washout` session, RSI14 24.67 (more oversold than last note), still below both moving averages, `regime_verdict` confirmed, still the board's lone value-trap name. **FHB** fell -1.41% (week -2.06%, month -9.00%), `rvol` 1.028, `divergence` quiet, RSI14 25.42, still below both SMAs, and still carries `reversal_tension`. **BOH** fell -0.99% (week -0.39%, month -5.34%), `rvol` 0.927, `divergence` quiet, RSI14 32.34, and is newly flagged with `reversal_tension` today (`mom_12_1` +21.1% against `ret_1m` -5.3%). **MATX** fell -2.47% (week -1.60%, month +5.89%) on `rvol` 0.934, `divergence` weak (contracting volume trend), still holding the strongest Hawaii-tier momentum rank (0.8 watchlist, 0.952 against the S&P 500).

---
Tracker context, not trading advice. Nothing here is a recommendation to buy or sell any security.
