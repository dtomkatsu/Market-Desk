# 2026-09-15

Breadth weakened for a third straight session (13 up, 29 down) as **RMIX** joined **GLD** and **HE** in `washout`, while **IOT** printed a second straight `confirmed` divergence — but today's was on a decline, not the advance that first earned the label on 9/14.

## Volume

**IOT** stayed `confirmed`: day -2.936%, week +3.736%, month +4.675%, `rvol` 1.085, `volume_trend` 1.232 (still expanding, up from 1.212 on 9/14), `up_down_ratio` 1.133, RSI14 56.91. On 9/14 this verdict confirmed an +11.8% advance on expanding volume; today the same verdict sits on a pullback with volume trend still rising — worth flagging as a change in character even though the label held.

**RMIX** newly reads `washout` — not called out in either of the last two notes: day -0.527%, week -16.392%, month -28.479%, `rvol` 1.22, `volume_trend` 1.256 (expanding), `up_down_ratio` 0.364 (the board's most down-heavy), RSI14 24.18 (oversold), `range_52w_position` 0.246. Decline-on-heavy-volume, the pattern the heuristic is built to flag negatively.

**HE** held `washout` for a fourth straight session: day -1.448%, week -6.33%, month -12.958%, `rvol` 0.926, `volume_trend` 1.212 (still expanding), `up_down_ratio` 0.436, RSI14 21.3 (lower than 9/14's 23.01), `range_52w_position` 0.019 — back to essentially a fresh 52-week low. Still the board's lone `value_trap` name. More in Hawaii tier below.

**GLD** held `washout` for a second straight session despite a small up day: +0.333%, week -1.393%, month -1.826%, `rvol` 0.55 (well below its 20-day average), `volume_trend` 1.335 (still elevated), `up_down_ratio` 1.596.

`weak` (advance-on-contracting-volume, or in MSFT's case a decline on the same contracting trend) narrowed to three names from five on 9/14: **AAPL** (-0.522% day, week +4.781%, month +8.306%, `rvol` 0.737, `volume_trend` 0.8) and **MSFT** (-1.64% day, week +0.642%, month +0.536%, `rvol` 0.842, `volume_trend` 0.595 — still the board's lowest, `up_down_ratio` 1.944 — still the board's highest). **MATX** also held `weak` — see Hawaii tier. **CMPS** and **SPCX** both cleared back to `quiet`.

Across the 42-symbol board: 1 `confirmed` (IOT), 3 `washout` (GLD, HE, RMIX), 3 `weak` (AAPL, MATX, MSFT), 16 `mixed`, 19 `quiet`. Versus 9/14: `washout` 2→3 (RMIX joined), `weak` 5→3 (CMPS, SPCX cleared), `mixed` 15→16, `confirmed` and `quiet` unchanged at 1 and 19.

Breadth continued weakening: 13 up, 29 down, none flat — down from 9/14's 16-up/26-down split and further from 9/11's 32-up/10-down rally.

## Valuation

Cheap end (tracked universe) is unchanged in order and essentially flat: **HE** (trailing P/E 7.93, `pe_percentile` 0.04), **FHB** (11.135, 0.08), **SU** (13.311, 0.12), **BOH** (13.914, 0.16) — all four moved less than 0.11 in trailing P/E from 9/14, none crossed a percentile.

Rich end unchanged in membership: **IOT** (1.0, Technology, trailing P/E 271.067, down from 277.667), **TSLA** (1.0, tracked universe, trailing P/E 334.654, up from 330.167) and **TT** (1.0, Industrials, trailing P/E 31.469, essentially flat from 31.5) still tied at the top of their respective peer groups.

Widest trailing-to-forward P/E gaps, with two order swaps from 9/14: **IOT** 271.067 → 42.823 (gap 228.2, narrowing further from 233.5 — a third straight session of narrowing). **TSLA** 334.654 → 165.887 (gap 168.8, widened from 165.0). **AMD** 130.407 → 32.914 (gap 97.5, widened slightly from 96.9). **ALAB** 133.257 → 42.102 (gap 91.2, widened from 84.9 — enough to move ahead of MCHP). **MCHP** 102.338 → 15.244 (gap 87.1, narrowed from 89.5). **DGII** 55.659 → 22.391 (gap 33.3, widened slightly, moving ahead of HLIT). **HLIT** 46.458 → 13.804 (gap 32.7, narrowed from 34.0). **MU** 20.963 → 5.927 (gap 15.0, flat).

## Factors

`value_trap` is unchanged: **HE** remains the only name flagged, watchlist-relative (`value_score` 0.978, `quality_score` 0.248, bottom-third). Against the S&P 500 Utilities benchmark (n=31), `bench_value_trap` is still false — `bench_value_score` 0.983, `bench_quality_score` 0.4, essentially unchanged from 9/14, mid-pack within-sector. A trap flag reads as caution, not a case for either side of a trade.

`reversal_tension` widened to twelve names from eleven on 9/14: **AMZN** (`mom_12_1` +15.1% vs `ret_1m` -5.4%), **BOH** (+21.5% vs -5.2%) and **GHRS** (+147.0% vs -6.6%, re-flagged after clearing on 9/14) newly carry it, while **LLY** and **TSLA** cleared. **ALAB, DGII, FHB, HLIT, MCHP, META, NVDA, NXT, TT** carry the flag unchanged.

Momentum extremes are unchanged in ranking: **MU** still tops both `mom_rank` (1.0) and `bench_mom_rank` (0.998, full S&P 500 cross-section), with `mom_12_1` easing further to +519.0% (from +531.8% on 9/14, continuing its multi-session decline). **META** remains at the bottom (`mom_rank` 0.0, `bench_mom_rank` 0.094, down from 0.106), `mom_12_1` -21.7% — more negative than 9/14's -20.5%, reversing that session's improvement. Quality extremes also unchanged: **MU** (0.922 watchlist, 0.927 within Technology) narrowly ahead of **NVDA** (0.921 / 0.926); **CMPS** still lowest in both populations (0.073 watchlist, 0.15 vs the S&P 500).

## What changed since 2026-09-14

- Breadth weakened for a third straight session: 13 up, 29 down, versus 9/14's 16-up/26-down.
- **RMIX** newly joined `washout` — decline-on-heavy-volume, `rvol` 1.22, RSI14 24.18 (oversold), `range_52w_position` 0.246.
- **IOT** held `confirmed` for a second session, but today's reading sits on a -2.9% decline rather than the +11.8% advance that first earned the label.
- **HE** extended `washout` to a fourth straight session, back near a fresh 52-week low (`range_52w_position` 0.019, RSI14 21.3).
- `weak` narrowed from five names to three: **CMPS** and **SPCX** cleared back to `quiet`.
- **IOT**'s trailing-to-forward P/E gap narrowed for a third straight session (233.5 → 228.2); **ALAB** and **DGII** each widened enough to swap order ahead of **MCHP** and **HLIT** respectively.
- `reversal_tension` widened from eleven names to twelve: **AMZN**, **BOH** and **GHRS** newly flagged; **LLY** and **TSLA** cleared.
- **MU**'s `mom_12_1` continued easing (+531.8% → +519.0%); **META**'s turned more negative again (-20.5% → -21.7%).
- `value_trap` and quality extremes are otherwise unchanged from 9/14.

## Hawaii tier

**HE** is the story again — see Volume above: `washout` for a fourth straight session, day -1.448%, week -6.33%, month -12.958%, `up_down_ratio` 0.436, `volume_trend` still expanding at 1.212, RSI14 21.3, `range_52w_position` 0.019 (essentially a fresh 52-week low), still the board's lone `value_trap` name, `reversal_tension` still false. **FHB** rose +0.272% (week +0.155%, month -5.657%), `rvol` 1.196, `divergence` quiet, RSI14 40.94, still below both SMAs, still carrying `reversal_tension`. **BOH** fell -0.75% (week -0.998%, month -5.194%), `rvol` 0.924, `divergence` quiet, RSI14 39.28, above its 200-day but not its 50-day average, newly carrying `reversal_tension` (`mom_12_1` +21.5% vs `ret_1m` -5.2%). **MATX** rose +0.837% (week +6.857%, month +9.802%) on `rvol` 1.772, `divergence` weak (advance on contracting volume, `volume_trend` 0.843), RSI14 73.0 — further into overbought territory — and `range_52w_position` 0.985, essentially at a fresh 52-week high. Still holding the strongest Hawaii-tier momentum rank (0.8 watchlist, 0.96 against the S&P 500).

---
Tracker context, not trading advice. Nothing here is a recommendation to buy or sell any security.
