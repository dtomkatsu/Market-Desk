# 2026-09-10

The board printed its broadest decline of this note series to date — 8 up, 34 down, none flat, matching 9/1's breadth exactly and well past 9/8's 30-down session — as **GLD**, **IOT** and **HE** all read `washout` simultaneously for the first time in this series.

## Volume

**HE** is back in `washout` after breaking to `mixed` on 9/8: `rvol` 1.725 (third-highest on the board), `volume_trend` 1.169 (expanding), `up_down_ratio` 0.481 (still down-heavy) — despite today's own +1.355% print, week is -4.384% and RSI14 sits at 24.25, still below both moving averages. More in Hawaii tier below.

**GLD** reads `washout` for the first time in this note series — previously a `confirmed` name (streak broke to `quiet` on 9/8): day -1.733%, week -1.594%, `rvol` 0.823 (below its 20-day average), `volume_trend` 1.329 (expanding), `up_down_ratio` 1.684.

**IOT** also newly reads `washout`: day -0.543%, week +4.485%, `rvol` 0.884, `volume_trend` 1.164 (expanding), `up_down_ratio` 1.312. This is the same name that printed the series' highest single-day `rvol` (4.542 on 9/4, 3.192 on 9/3) while reading `quiet` both times — today it's a smaller `rvol` day but the multi-week volume trend has caught up to the price weakness.

**CMPS** posted the board's largest single-day decline, -6.262%, on `rvol` 1.321 but `volume_trend` 0.715 (contracting) — `divergence` reads `quiet`, drift rather than distribution. **ALAB** (-5.327%, `rvol` 0.912, `volume_trend` 0.725) also reads `quiet`. **MU** (-4.9%) and **AMD** (-3.358%) both read `weak`, `volume_trend` 0.651 and 0.707 respectively. **RMIX** (-3.857%, `rvol` 1.546, `volume_trend` 1.061) reads `mixed`.

**AAPL** was the board's only name up more than 2% today (+3.561%, week +0.495%) on `rvol` 1.72, but `volume_trend` is 0.774 (contracting) — `divergence` reads `weak`, an advance not backed by trend-level participation.

**TLT** printed the board's highest `rvol` at 2.124 on a -1.162% day, but reads `quiet`. **JETS** followed at `rvol` 1.854 (-0.642%), reading `mixed`.

Across the 42-symbol board: 0 `confirmed`, 3 `washout` (GLD, HE, IOT), 4 `weak` (AAPL, AMD, MATX, MU), 14 `mixed`, 21 `quiet`. Versus 9/8: `washout` 0→3, `weak` 5→4, `mixed` 13→14, `quiet` 24→21 — the extreme categories that emptied out together on 9/8 both refilled today, and on the `washout` side alone.

## Valuation

Cheap end is unchanged in both order and percentile from 9/8, with trailing multiples drifting slightly: **HE** (trailing P/E 8.116, `pe_percentile` 0.04, tracked universe), **FHB** (11.161, 0.08, tracked universe), **NXT** (21.021, 0.091, Technology), **SU** (12.718, 0.12, tracked universe), **BOH** (14.093, 0.16, tracked universe), **MU** (22.118, 0.182, Technology).

Rich end also unchanged: **IOT** (1.0, Technology, trailing P/E 256.267), **TSLA** (1.0, tracked universe, trailing P/E 339.776) and **TT** (1.0, Industrials, trailing P/E 32.799) still tied at the top of their respective peer groups.

Widest trailing-to-forward P/E gaps are the same cluster as 9/8, with **AMD** and **ALAB** swapping order: **IOT** 256.267 → 40.747 (gap 215.5), **TSLA** 339.776 → 168.426 (gap 171.4), **AMD** 128.798 → 32.218 (gap 96.6), **ALAB** 139.476 → 44.503 (gap 95.0), **MCHP** 105.265 → 15.68 (gap 89.6), **HLIT** 48.5 → 14.411 (gap 34.1), **DGII** 54.579 → 21.957 (gap 32.6), **MU** 22.118 → 6.305 (gap 15.8).

## Factors

`value_trap` is unchanged: **HE** remains the only name flagged, watchlist-relative (`value_score` 0.978, `quality_score` 0.248, bottom-third). Against the S&P 500 Utilities benchmark, `bench_value_trap` is still false — `bench_value_score` 0.983, `bench_quality_score` 0.4 sits mid-pack within-sector.

`reversal_tension` narrowed to eleven names from twelve on 9/8: **GOOG** (`mom_12_1` +43.2% vs `ret_1m` -3.6%) and **HE** (`mom_12_1` 0.0% vs `ret_1m` -11.6%) cleared, while **META** is newly flagged — its `mom_12_1` sits at -21.5% but `ret_1m` turned positive at +7.55%, the down-trend-meets-up-month pattern. **ALAB, AMZN, DGII, FHB, GHRS, LLY, MCHP, NXT, TSLA, TT** carry the flag unchanged.

Momentum extremes are unchanged: **MU** still tops both `mom_rank` (1.0) and `bench_mom_rank` (1.0, full S&P 500 cross-section), `mom_12_1` +543.2% (down from +569.1% on 9/8, continuing to ease). **META** remains at the bottom (`mom_rank` 0.0, `bench_mom_rank` 0.102), `mom_12_1` -21.5%. Quality extremes also unchanged: **MU** (0.922 watchlist, 0.927 within Technology) narrowly ahead of **NVDA** (0.921 / 0.926); **CMPS** still lowest in both populations (0.073 watchlist, 0.154 vs the S&P 500).

## What changed since 2026-09-08

- The board turned sharply lower again: 8 up, 34 down, none flat — the broadest decline in this note series, matching 9/1's 34-down count and exceeding 9/8's 30-down session.
- **GLD**, **HE** and **IOT** all read `washout` today — the first time three names have carried that verdict simultaneously in this series. **HE** returns to `washout` after one session at `mixed`; **GLD** and **IOT** are printing it for the first time (GLD was previously a `confirmed` name; IOT had twice printed the series' highest `rvol` while reading `quiet`).
- Divergence mix shifted accordingly: `washout` 0→3, `weak` 5→4, `mixed` 13→14, `quiet` 24→21, `confirmed` unchanged at zero.
- `reversal_tension` narrowed from twelve names to eleven: **GOOG** and **HE** cleared; **META** newly flagged.
- Cheap-end and rich-end valuation percentiles, and momentum/quality extremes, are otherwise unchanged from 9/8.

## Hawaii tier

**HE** is the story again — see Volume above: back in `washout` after one session at `mixed`, `up_down_ratio` 0.481, `volume_trend` still expanding at 1.169, RSI14 24.25, still below both moving averages, still the board's lone `value_trap` name, and `reversal_tension` cleared today (`mom_12_1` flattened to 0.0%). **FHB** rose +0.234% (week -0.311%, month -5.666%), `rvol` 1.097, `divergence` quiet, RSI14 37.14, still below both SMAs, still carrying `reversal_tension`. **BOH** rose +0.57% (week -0.902%, month -3.672%), `rvol` 0.775, `divergence` quiet, RSI14 40.96, above its 200-day but not its 50-day average, no `reversal_tension`. **MATX** rose +0.498% (week +4.482%, month +11.536%) on `rvol` 1.106, `divergence` weak (advance on contracting volume), still holding the strongest Hawaii-tier momentum rank (0.8 watchlist, 0.944 against the S&P 500).

---
Tracker context, not trading advice. Nothing here is a recommendation to buy or sell any security.
