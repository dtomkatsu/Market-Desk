# 2026-08-24

Gold is the only name on the 42-symbol board throwing a `confirmed` divergence today — up on volume nearly double its 20-day average — while the cheap and expensive ends of the valuation board haven't moved an inch since the last note four sessions ago.

## Volume

**GLD** carries both the highest `rvol` on the board (1.935) and the session's only `confirmed` divergence verdict: +0.79% on the day, +5.23% for the week, +14.73% for the month, `up_down_ratio` 1.889, RSI14 72.49. The two prior notes flagged GLD for heavy turnover without confirmation (`rvol` 2.014 and 1.549, both reading `weak`); today the volume finally lines up with sustained price strength enough to clear the confirmed bar. Worth repeating: `divergence` is a descriptive heuristic here, not a backtested signal.

**DGII** is still working through elevated-volume selling, though it's moderating rather than deepening: `rvol` 1.586 today (second-highest on the board), -1.32% on the day, but the weekly loss has narrowed to -11.3% (from -13.54% as of 2026-08-20) and the monthly figure is back to +10.41%. Still reads `mixed`, not `confirmed`, on divergence.

**COST** flip-flopped again. The last note caught it reversing from a light-volume advance to a heavy-volume decline; today it's up +2.5% on `rvol` 1.18 with `volume_trend` 0.804 (contracting) — an advance without full participation behind it, `weak` divergence, the same "up on light volume" pattern from two notes ago rather than the heavy-volume decline seen most recently.

**MSFT** again posted the lowest `rvol` on the board (0.457, versus 0.534 and 0.526 in the two prior notes) while extending its monthly gain to +27.91% (from +23.26%) — still climbing on progressively lighter volume (`volume_trend` 0.909), now the most pronounced instance of that pattern yet.

**MATX** picked up unusual turnover for an otherwise flat day: `rvol` 1.547 against a -0.05% daily change, reading `mixed`. Elevated volume with essentially no net price move.

No name posted a `washout` verdict today; 21 of 42 symbols read `mixed`, 11 `quiet`, 9 `weak`, and GLD alone `confirmed`.

## Valuation

The cheap and rich ends of the board are identical to 2026-08-20's note, four sessions ago. Cheapest: **HE** (trailing P/E 8.845, `pe_percentile` 0.04, tracked universe), **FHB** (0.08), **MU** (0.091, within Technology), **SU** (0.12), **BOH** (0.16) — same five names, same order, same percentiles. Richest: **IOT** (1.0, Technology, trailing P/E 403.3) and **TT** (1.0, Industrials, trailing P/E 33.75) tied at the top of their peer groups, with **TSLA** (0.96, tracked universe, trailing P/E 320.14) close behind.

Trailing-to-forward gaps are also stable, just drifting with price: **MCHP** 109.13 → 16.26, **ALAB** 137.39 → 43.41, **AMD** 116.22 → 29.49, **TSLA** 320.14 → 161.66, **MU** 20.56 → 5.87. Same five names carrying the size of gap; nothing has re-rated since the last note.

## Factors

`value_trap` is unchanged: **HE** remains the only name flagged true, watchlist-relative (cheapest print, `quality_score` 0.248, bottom-third). Against the S&P 500 Utilities benchmark (n=31), `bench_value_trap` is still false for HE — `bench_quality_score` 0.4 sits mid-pack within-sector even though it's bottom-third against this watchlist. Same population disagreement noted before, still holding.

`reversal_tension` shifted slightly: today's list is **AAPL, AMD, BOH, FHB, HE, MCHP, MSFT, NXT, TSLA, TT** (10 names, up from 9). **AAPL** (`mom_12_1` +48.5% vs `ret_1m` -6.73%) and **TT** (`mom_12_1` +14.7% vs `ret_1m` -5.83%) are newly flagged; **ALAB** cleared (no longer fighting its 12-1 trend).

Momentum and quality extremes are unchanged: **MU** still tops both `mom_rank` (1.0) and `quality_score` (0.922) watchlist-side, and `bench_mom_rank` (1.0) against the full S&P 500; **MSFT** still sits at `mom_rank` 0.0 / `bench_mom_rank` 0.12 despite the ongoing rally, since the 12-1 window excludes the reversal-prone last month; **CMPS** still holds the lowest `quality_score` in either population (0.073 watchlist, 0.154 against the S&P 500). On within-sector quality, **NVDA** (`bench_quality_score` 0.942) still edges out MU (0.93) once measured against the full Technology sector.

## What changed since 2026-08-20

- **GLD** graduated from a `weak`-divergence heavy-volume print to today's `confirmed` verdict — the first confirmed divergence in this note series.
- **DGII**'s heavy-volume selloff moderated: weekly loss narrowed from -13.54% to -11.3%, still on above-average turnover, still `mixed`.
- **COST** reversed again, back to a light-volume advance after last note's heavy-volume decline.
- **MSFT**'s light-volume climb extended further (monthly gain now +27.91%, `rvol` down to 0.457).
- `reversal_tension` picked up **AAPL** and **TT**, dropped **ALAB**.
- Valuation percentiles at both ends of the distribution, and the `value_trap`/`bench_value_trap` split on HE, are unchanged.

## Hawaii tier

Quiet by comparison to the rest of the board. **HE** keeps deepening: monthly loss now -15.48% (from -14.63%), RSI14 down to 27.16, still below both its 50- and 200-day averages, `regime_verdict` confirmed, and it remains the board's lone value-trap name. **FHB** eased slightly (-6.49% month vs -6.76%) but is now below its 200-day average as well as its 50-day, which it wasn't in the last note. **BOH** ticked down modestly (-8.48% month). **MATX** was the volume outlier described above but essentially flat on price (-0.05% day), still holding the strongest Hawaii-tier momentum rank (0.84).

---
Tracker context, not trading advice. Nothing here is a recommendation to buy or sell any security.
